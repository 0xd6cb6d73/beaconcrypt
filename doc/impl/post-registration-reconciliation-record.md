# Post-registration fidelity reconciliation

## Current baseline and ownership

The isolated `codex/vcvio-proverif-post-registration-ratchet` branch starts at `4c718ffe346cce3af1ceab3c48d5783d1c1bc813`. On 2026-09-05 the registration worktree was clean at `782265b`, and that commit was confirmed as an ancestor of fetched `origin/vcvio`. Direct `git ls-remote origin refs/heads/vcvio` returned `e5a361289191dbb7bab3675d42d688396044d91a`. The M3 implementation and adapter fixture synchronization were therefore durable before this reconciliation began. An immediate `git fetch origin vcvio` preceded the ordinary, uncommitted merge of that remote checkpoint. No other worktree was edited.

The fidelity lane owns its Rust fixture reconciliation and this record. The integration owner resolves the Makefile and maintained analysis conflicts, reviews the combined checker, updates the trust-boundary inventory, and performs integrated verification and complete implementation mutation testing before commit and push.

## Semantic reconciliation

M3's main baseline adds replicated first- and third-sequence completion schedules, a cryptographic-session correspondence that excludes mutable response/routing fields, and eight controls including accepted outer-ID relabelling. The isolated post-registration scenario retains its stricter same-assigned-ID sequence-three substitution and private committed-state handoff. Its cached sequence-one delivery, identical replay, and genuine sequence-four continuation remain a separate finite execution. The combined branch does not claim that this isolated continuation extends every M3 completion or the relabelled routing case.

The incoming raw-receive changes use `decode_crypto_frame`, validated payload slices, and the owned `execute_open` request. Their exact source fixtures and six additional CryptoFrame mutations merge with the post-registration checks. The core ratchet state-machine behavior used by the retained runtime witness is unchanged: cached success consumes only its selected material, replay rejects before a KDF or open, and future receive derives from the carried chain. The fixture retains the actual returned kernel throughout and observes counters, chains, cache length, and all 50 public cache-entry results. It does not inspect inactive array bytes or establish physical erasure.

Both branches added one Make scenario to their common ancestor. The combined count is 30. The new main-registration target also falls between the post-registration and passive-equivalence targets, so the post-registration fixture's target boundary moves to that main-registration target. These changes preserve exact per-target occurrence checks. The Rust resolution changes four existing lines: the scenario count, its diagnostic, the corresponding legacy mutation diagnostic, and the post-registration target boundary. No model equation, query term, checker polarity, or mutation count is relaxed to accommodate the merge.

The 409-case later-registration and 297-case post-registration gates remain independently counted. All 47 post-registration facts, all 27 query hashes, and the exact post-registration result-checker hash remain unchanged. The integrated CryptoFrame gate retains M2's 229 cases. The combined manifest contains 512 facts and the full fixture contains 3,164 deliberate in-memory mutations; these are source/model maintenance tripwires, not formal Rust-to-ProVerif semantic refinement.

The integration review also found that the CI matrix omitted the legacy later-sequence registration, main registration controls, and post-registration ratchet targets even though Make included them. All three existing generated-proof targets are now in CI. The matrix has exactly the same 30 unique ProVerif scenarios as Make, and inventory guards require each registration target once without changing the workflow triggers or required aggregate check. The refreshed inventory preserves the independent 409/18 legacy gates, adds the 297/27 continuation gates, and records the exact private channels and library load order.

## Validation and assumptions

`CARGO_TARGET_DIR=/tmp/beaconcrypt-proverif-reconcile-target cargo test --locked -p beaconcrypt-core --test proverif_transcript_fidelity` passes all 16 tests after the four-line reconciliation. The combined tests include the preserved 409- and 297-case gates, M3's main-schedule prefix checks, and both concrete retained-state witnesses. The initial merged runs correctly rejected the temporary unresolved Make target layout; no failing check was disabled. `cargo fmt --all --check`, `git diff --check`, and `git diff --cached --check` pass. `CARGO_TARGET_DIR=/tmp/beaconcrypt-proverif-reconcile-target cargo clippy --locked -p beaconcrypt-core --test proverif_transcript_fidelity` passes with the 35 existing core warnings.

The cryptographic components remain ideal implementations satisfying their stated contracts. This reconciliation adds no primitive assumptions or cryptographic implementation proofs. Its symbolic continuation uses the existing ideal HKDF, ChaCha20-Poly1305 and BLAKE2b commitment equations and the existing strong assigned-ID theory for its delimited original-assignment case. Exact constructor equalities do not establish numerical collision bounds, arbitrary production schedules, multiuser composition, parser/compiler correspondence, persistence, application routing, or physical erasure. The runtime witness supplies opaque KDF and opening replies and checks protocol state transitions under those replies.

The integration owner completed the remaining acceptance checks. The focused 27-query post-registration and legacy 18-query later-registration checks pass with every exact classification unchanged. `make -C beaconcrypt-core check-generated SHELL=/run/current-system/sw/bin/bash PROVERIF_TIMEOUT=900` invokes the required locked `verify` boundary and passes all 30 listed ProVerif scenarios plus two auxiliary groups, the SSProve compilation/assumption/kernel checks, both regenerated extractions, nine panic-coverage checker regressions, all 269 exact operation certificates, and the full 3,168-job Lean build. Generated artifacts are byte-for-byte unchanged. The separate final `make -C beaconcrypt-core check-inventory SHELL=/run/current-system/sw/bin/bash` passes after reviewing all changed fingerprints and the three added finite-model files.

The all-feature workspace Rust suite passes 285 tests; full all-target/all-feature workspace Clippy, formatting, and both no-default-feature role checks pass. Existing unrelated core lint warnings remain. The complete configured `cargo mutants --workspace --jobs 6` run finished all 1,173 cases in 36 minutes: 816 caught, 357 unviable, zero missed and zero timed out. Mutation configuration and exclusions are unchanged. Runtime sources are identical to `e5a3612`; the new fidelity coverage is additional validation, not a runtime or wire-format change.

Validation used Rust 1.96, Cap'n Proto 1.1.0 and the existing `RUSTFLAGS='-C linker-features=-lld'` host workaround. The locked proof shell was restored after earlier garbage collection; its first sandboxed download attempt failed, and the authorized restoration succeeded. An initial 240-second M3 run stopped with incomplete output under concurrent toolchain rebuilding; the unchanged eight controls pass in the final aggregate with the explicit 900-second per-scenario limit. Two mutation startup attempts stopped before any mutants because of missing host tool selection/build tools; only the complete passing run is acceptance evidence.

Independent semantic review verified the full finite trace, exact query hashes, both mutation gates, unchanged M3 agreement distinctions, inventory additions, and equality of the 30 Make/CI scenario sets. The checkpoint is integration-ready for its stated finite fidelity boundary. Logs are retained under `/tmp/beaconcrypt-staged-*`; publication records the local, fetched and direct private-remote object equality. No Nix garbage collection is permitted while the parallel Lean checkpoint still uses the store.
