<!-- SPDX-License-Identifier: 0BSD -->

# Reviewed formal-verification trust boundary

This is the canonical Stage 9 inventory for the beaconcrypt proof boundary.
It records the production behavior that remains opaque to extraction, the laws
used to relate that behavior to the proofs, the adapter refinements required by
the theorems, the trusted proof-library surface, generated-backend exceptions,
and every repository-owned handwritten backend review unit.

The production ratchet has since replaced stored executors and seal/open callbacks with typed affine effects. The current Rust boundary is described in OR-07 through OR-15 and AR-01 through AR-04 below. The checked concrete F* artifacts in GE-01, HB-01, and HB-02 still describe the predecessor executor/callback source snapshot and are retained as historical proof evidence only; they do not verify the new effect API. The default core now translates to Lean without module exclusions. Its control-plane refinement targets the ideal `Ratchet.recvStep` directly at the common 50-skipped-key bound, and the imported effect-refinement file proves the precise kernel relation, an assumed response law, non-exhausted send begin/resume/ideal success plus cancellation, rollback for supplied finite failed-trace witnesses, conditional open-reply consistency, generated cached-open construction from the kernel relation plus an ideal lookup, the control-plane half of cached consumption, and a conditional cached-success path. The imported ideal PQXDH model and its extracted-code refinement cover the deterministic registration surface summarized in HB-44 and HB-45. The native VCVio theorem in HB-46 additionally reduces the ideal record model's concrete CTX misattribution event losslessly to collision resistance of its pure BLAKE2b function. Production CTX transcript/hash correspondence, future receive publication, cached material-array publication, initial role composition, restoration, the synchronous driver, and ideal send exhaustion remain explicit obligations.

The accompanying `reviewed-inventory.txt` fingerprints the files that realize
this inventory. `make check-inventory` checks those fingerprints, derives the
complete handwritten/generated file sets, and checks the structural exception
counts below. A changed fingerprint is a review tripwire: it forces an
intentional manifest change into the same review, but it cannot establish that
a human performed a sound review.

## Boundary baseline

The reviewed baseline has:

- thirteen `adapter-rust` whole-file review units: `../beaconcrypt/build.rs` plus the twelve `../beaconcrypt/src/*.rs` files, including the `../beaconcrypt/src/persistence.rs` boundary;
- nine `core-rust` review units: the five crate-root source files plus `src/ratchet/{control,refined,concrete}.rs` and `src/pqxdh/concrete.rs`; the two formerly excluded concrete modules are now inside the Lean translation boundary;
- no `hax_lib::opaque` or `hax_lib::opaque_type` annotation in the protocol
  core;
- five generated F* implementation modules and eight handwritten F* files: three strictly checked proof implementations and five interfaces;
- one no-exclusion generated Lean extraction, with its generated forwarding/scaffolding modules and every inventoried handwritten model, refinement, computational, assumption, and proof-root file reachable from the maintained root; this includes checked structural effect laws, phase-to-ideal refinement theorems, the ideal PQXDH model, and the VCVio CTX reductions;
- one generated ProVerif library, exactly three production-backed replacement fragments, thirty-five handwritten ProVerif model/query/interface libraries, eighteen top-level process files, and one result classifier;
- three Rust validation units, including the dependency-free production transcript fidelity checker;
- eighteen repository-owned SSProve review units containing the exact production-label contract, bounded hidden-ROM runner, CTX event reduction, CTX binding and privacy games, closed four-modality ideal protocol game, a single-run consuming CKEY composition seam with an internally sampled optional-ciphertext public view, its exact one-call package certificate, a bounded two-handle adaptive-context relation with one globally cached finite sample, attacker-facing bounded protocol-ROM hop, hybrid-combiner robustness game, one-step erasure-conditioned ratchet game, combined-authenticator record-integrity game and fresh one-bit numerical bound, exact assumption allowlist, locked build entry point, namespace configuration, and scope documentation;
- no local F* `assume` or `admit`, no lax checking, and no unresolved generated
  backend placeholder; and
- one handwritten ProVerif result classifier that is trusted to distinguish
  required positive results, reachability witnesses, and deliberate negative
  compromise results.

The core owns deterministic protocol decisions. Cap'n Proto, serde,
libsodium, ML-KEM, allocation, entropy, persistence mechanics, erasure, and
language bindings remain outside extraction.

## Opaque Rust and adapter functions

The entries below are the repository-owned production wrappers in the claimed high-level trace. The named external calls are opaque: neither hax backend extracts their Rust implementation.

| ID | Production review unit | Opaque behavior and proof relevance |
| --- | --- | --- |
| OR-01 | `Server::new` and `Beacon::new` | Initialize libsodium and construct role-specific unestablished state. `Server::new` generates an Ed25519 identity or derives it from the supplied seed, initializes the verified server counter, and starts with empty established-remote and replay sets. `Beacon::new` parses and stores the mandatory server Ed25519 key, generates a fresh beacon identity, constructs the proof-visible `ServerBinding` from that public key and numeric identity-key ID, and generates its X25519 prekey and ML-KEM keypair; no operational ratchet exists before establishment. The primitive calls are outside the proof. Persistent creation and restoration are OR-14. |
| OR-02 | `<Beacon as ProviderBeacon>::get_registration_bundle` and `Beacon::{new_onetime_keypair,delete_onetime_keypair,delete_pq_keypair,abort_registration}` | Supply fresh one-time coins, map the core `InitKex` fields to Cap'n Proto, sign the complete role-tagged X25519 and typed ML-KEM payloads with `crypto_sign::sign`, and perform the consuming or terminal production phase change. The public deletion helpers are compatibility/abort paths, not additional successful traces. |
| OR-03 | `<Beacon as ProviderBeacon>::finish_registration` | Parses the response, passes its Ed25519 identity to the core comparison against the binding retained from construction, converts that response key to X25519, decapsulates ML-KEM, computes ordered DH1--DH4, derives the root and initial chains, opens the first record using the retained numeric server ID, and passes the successfully opened sender ID and assigned-ID prefix to the core authentication transition. It rechecks the stored server public key against the retained binding and publishes the assigned identity, associated data, and staged ratchet only by entering `BeaconState::Established`; failure enters terminal aborted state with no operational ratchet. |
| OR-04 | `<Server as ProviderServer>::get_shared_secret` | Parses `InitKex`, calls `crypto_sign::verify` three times, validates role tags, classifies and consumes the semantic replay ID, creates ephemeral/KEM coins, performs the complementary four DH operations, and derives the root. |
| OR-05 | `<Server as ProviderServer>::build_registration_response` | Refines key-ID availability, initializes the complementary ratchet off-map, seals the authenticated assigned-ID prefix, and serializes the response. Every recoverable `None`/`?` failure occurs before the final commit section, which publishes an `EstablishedRemote` and server counter sequentially as one logical transition; `PersistentServer` separately wraps the complete result in the external durable CAS of OR-14. F* does not prove either adapter mutation, crash behavior, or concurrency. |
| OR-06 | `shared_secrets` and `derive_root_key_input` in `../beaconcrypt/src/pqxdh.rs` | Translate the five concrete shared secrets without reordering and call `crypto_kdf::hkdf::sha512::{extract,expand}` with `PQXDH_INFO`; the transcript is erased after the call. The test-only `derive_root_key` uses the same wrappers. |
| OR-07 | `symmetric_ratchet_hkdf` and `ratchet_hkdf` | `ratchet_hkdf` receives only a core-owned `SymmetricRatchetKdfRequest`, executes HKDF-SHA-512, and wraps the 76-byte result in `RatchetKdfResponse`. The core constructs the exact current-chain plus `SYM_RATCHET_INFO` request and privately splits the response into `0..32` key, `32..64` next chain, and `64..76` nonce. No executor or function pointer is stored or passed through the core. The adapter must invoke the effect exactly once and return the matching response; HKDF semantics/totality, output noncollision, adapter panic/side effects, compiler correspondence, and physical erasure remain external. |
| OR-08 | `initial_ratchet_hkdf`, `finish_initial_ratchet_kdf`, and the registration calls to `start_beacon_candidate_ratchet_kdf` and `start_server_candidate_ratchet_kdf` | The core returns a non-clonable `InitialRatchetKdfPending` containing the exact agreed-root plus `SYM_RATCHET_INFO` request and role-ordering continuation. `finish_initial_ratchet_kdf` executes that request, wraps its 64-byte result in the distinct `InitialRatchetKdfResponse`, and consumes the continuation through `resume_initial_ratchet_kdf`; the core alone splits and orders the two 32-byte chains and returns `ConcreteRatchetKernel`. Agreement on the root remains conditional on AR-08. The typed response prevents 64/76-byte phase confusion but does not prove response provenance, HKDF semantics/totality, adapter panic behavior, compiler correspondence, or physical erasure. |
| OR-09 | `Server::encrypt_message`, `Beacon::encrypt_message`, `encrypt_message_with_ratchet`, `seal_frame`, and `<Server as ProviderServer>::{encrypt_and_update,encrypt_and_update_json}` | Server selects one `EstablishedRemote`; Beacon accepts only `BeaconState::Established`. The helper takes the affine kernel from `RatchetKernelSlot`, calls `begin_send` with owned `SealFrameContext`, executes the sole emitted request through OR-07, resumes `SendKdf`, and passes `SendSeal`'s exact material, sequence, and same opaque context to `seal_frame`. The primitive path performs ephemeral libsodium conversion, ChaCha20-Poly1305 sealing, CTX commitment, and frame serialization. `SendSeal::finish` returns the advanced kernel for both `Some` and `None`, preserving failed-send key consumption, and the helper reinstalls that kernel before normal return. Update wrappers expose only an inert `RatchetSnapshot`; `PersistentServer` withholds results until OR-14 CAS succeeds. Driver/primitive fidelity, establishment selection, codec/store correctness and provenance, panic/crash behavior, compiler correspondence, and physical erasure remain unproved. |
| OR-10 | `Server::decrypt_message`, `Beacon::decrypt_message`, `encrypted_frame_sender`, `decrypt_message_with_ratchet`, `open_frame`, and `<Server as ProviderServer>::{decrypt_and_update,decrypt_and_update_json}` | Server parses the sender ID and selects the matching `EstablishedRemote`; Beacon accepts only `BeaconState::Established`. The helper rejects malformed, wrong-sender, and short frames before taking the kernel and calling `begin_receive` with owned `OpenFrameContext`. It executes each emitted KDF request through OR-07; future derivations update only private continuation state. `ReceiveOpen` supplies the selected material, sequence, and same opaque context to `open_frame`, which checks CTX and decrypts through ephemeral libsodium values. Admission/internal rejection, KDF cancellation, explicit open rejection, and `finish(None)` return the exact entry kernel; only `finish(Some(plaintext))` publishes the prevalidated cached removal or future chain/skipped-cache/control delta. The helper reinstalls the returned kernel before normal return, and `Server` preserves accepted/rejected distinction for persistence. Parser/driver/primitive fidelity, establishment selection, codec/store correctness and provenance, panic/crash handling, external copies, and rollback remain unproved. |
| OR-11 | `build_commitment` | Checks/converts the concrete slices, passes `(key, nonce, associated data, AEAD tag, sequence, sender ID)` to the extracted core `build_commitment_transcript`, and calls libsodium BLAKE2b `crypto_generichash::generichash` over the returned 229 bytes. F* proves the helper's exact fixed-width order and both LE64 encodings; slice provenance, conversion, the opaque hash call, and compiled caller remain adapter obligations. `libsodium_rs::utils::memcmp` performs concrete secret/commitment comparison. |
| OR-12 | `RatchetKernelSlot`, `RatchetManager::{from_kernel,status,receive_cache_len,send_state,recv_state}` plus its `#[cfg(test)]` default/reset/raw receive helpers, Server test delegations, and the core's doc-hidden `begin_receive_advance` test utility under `cfg(any(test, feature = "test-utils"))` | Store one non-`Clone`, crate-private `ConcreteRatchetKernel` as authoritative state inside a private `Option` slot. Production takes the kernel only while synchronously interpreting one effect and must put the returned kernel back before normal return; phase values never escape or enter persistence. If interpretation panics, unwinding drops the phase and leaves the slot empty so later access fails closed, but panic/crash recovery is not a formal transaction guarantee. Production exposes copied status and inert serialized views but no live manager, raw material, pending phase, low-level advancement, reset, arbitrary completion, KDF label, or executor choice. Core secret state, continuations, and restore builders are non-`Clone`; this safe-Rust affine property is reviewed evidence, not a Lean/F* lineage theorem or proof that memory was never copied. Test helpers remain outside the claimed trace. |
| OR-13 | `Beacon::{associated_data,ratchet_status,identity_key_kid,server_id,server_kid,identity_pk,identity_sk,pq_pk,pq_sk}`, `Server::{ratchet_status,identity_key_kid,server_kid,pk_by_kid,identity_pk,identity_sk}`, `EstablishedRemote::{new,pk,ratchet,ratchet_mut}`, and crate-private/test-only accessors | Refine establishment-gated selection: only `BeaconState::Established` owns a beacon ratchet, and the Server map value type is `EstablishedRemote`. The production constructor for an established remote is crate-private and used on PQXDH commit or fresh restoration from the trusted store; manual insertion, deletion, reset, associated-data mutation, live-ratchet access, and compatibility allocation are test-only or crate-private. This is runtime/visibility enforcement, not a public generic typestate witness or a whole-map F* theorem. |
| OR-14 | `RatchetManager` serialization/deserialization, `serialize_server_state`, `deserialize_server_state`, the serde impls in `../beaconcrypt/src/ser.rs`/`../beaconcrypt/src/deser.rs`, and `ServerSnapshot`, `SnapshotHead`, `SnapshotStore`, `PersistentServer`, and the binding-only `BindingSnapshotStore` in `../beaconcrypt/src/persistence.rs` | Serde, the libsodium digest call, and persistence are not extracted. The internal ratchet payload has exactly `send_key`, `recv_key`, `send_ctr`, `recv_past`, and `recv_ctr`; no send capability, executor, effect phase, transaction, or `Option` slot is persisted. Output sorts cached sequences, peer IDs, and replay identifiers. A custom visitor observes raw map entries before insertion, requires canonical decimal `u64` spellings, and rejects duplicate parsed keys; it is the visitor, not `HashMap`, that establishes input uniqueness. Import rejects unknown or legacy fields, validates type/role encodings and bounds, and rebuilds direct chains/material only through the checked restoration builder, with no executor rebinding. Public updates contain an inert `RatchetSnapshot` with no live conversion. Full `ServerSnapshot` activation passes through `PersistentServer::restore`. The version-2 envelope contains magic, version, kind, reserved byte, lineage, generation, parent digest, payload length, and the exact canonical JSON payload; decode/re-encode must reproduce the payload byte-for-byte. The unkeyed 32-byte BLAKE2b digest identifies exact envelope bytes for `SnapshotHead` CAS and parent linking but never authenticates them. A conforming production `SnapshotStore` is the external trust anchor: `load` must return only protected exact bytes with required integrity and provenance, and CAS must compare the complete trusted head atomically and durably install the exact replacement bytes and new head before success while keeping the head rollback-resistant. Restoration advances generation by CAS before becoming live. Accepted receives and other state-changing operations withhold output until successor CAS; a CAS or successor-construction failure poisons the owner. Receive-specific `commit_receive` sets poison before calling the operation but clears it and returns rejection without generation arithmetic, snapshot encoding, digest calculation, store load, or CAS when `ReceiveTransition::Rejected` is returned. Post-commit update rendering can return `PersistenceError::OutputEncoding` while leaving the accepted target durably consumed and the owner usable. The C, Go, and Python export/from-state helpers instead use `BindingSnapshotStore`, whose CAS and checkpoint are only in memory; rejected receive leaves their exported bytes unchanged, but accepted output can escape before the caller durably saves the next export. Import trusts caller-supplied bytes as current, separate restorers can fork them, and these helpers do not discharge AR-18. Snapshots have no cryptographic authentication or encryption. The predecessor F* artifact proves only conditional `reachable_restore`, and the current phase-aware Lean restoration refinement is pending; serde correctness, digest semantics/collision resistance, canonical-codec fidelity, store integrity/provenance, crash durability, rollback resistance, and deployment coordination remain external. |
| OR-15 | `zeroize_shared_secrets`, `<SecretArr as From<Vec<u8>>>`, `<SecretArr as From<[u8; S]>>`, `SecretArr::{as_slice,copy_from_slice,inner}`, core request/response/chain/key/nonce/material zeroization derives, `Zeroize::zeroize`, and `Zeroizing` uses | Persistent chains and cached material, plus secret-bearing request and response wrappers, use `ZeroizeOnDrop`; dropping an owned continuation recursively drops its secret fields. Production creates libsodium key/nonce values only ephemerally during final seal/open interpretation, and temporary HKDF output uses `Zeroizing`. Best-effort physical erasure, register/stack/library copies, and compiler preservation remain external behavior; proofs claim only logical unavailability after the relevant transition. |
| OR-16 | `encode_sign`, `decode_sign`, Cap'n Proto readers/builders inside OR-02--OR-05, and `build_associated_data` | Validate or construct typed persistence/wire bytes and map them to the exact proof-visible fields. These conversions are adapter obligations, not cryptographic authentication by themselves. The `#[cfg(test)]`-only `encode_kem`/`decode_kem` helpers are monitored with their source file but are not production review units. |
| OR-17 | `memset_explicit`, the Windows-GNU `SystemFunction036` shim, and its external `BCryptGenRandom` call | Supply platform erasure/RNG compatibility below the primitive boundary. Their implementation, constant-time behavior, and OS entropy are outside the proof. |
| OR-18 | Every other function in `../beaconcrypt/build.rs` and the complete `../beaconcrypt/src/{beacon,cbinds,deser,error,lib,persistence,pqxdh,pybinds,ratchet,ser,server,shared}.rs` adapter set | The recursively checked whole-file review units classify all remaining schema generation, bindings, API forwarding, formatting, tests, and compatibility/direct low-level functions as outside the proof claim. This is the exhaustive catch-all for repository-owned Rust outside the isolated core, not an assertion that those functions are verified. |

The external primitives covered by these wrappers are Ed25519 signing and
verification, Ed25519-to-X25519 public/secret conversion, X25519 scalar
multiplication, ML-KEM-768 encapsulation/decapsulation, HKDF-SHA-512,
ChaCha20-Poly1305-IETF, BLAKE2b, OS-backed key generation, constant-time byte
comparison, Cap'n Proto parsing/serialization, serde, allocation, and
zeroization. Byte constructors such as `from_bytes` are wire-validation
operations, not evidence that the wire is authenticated.

For opacity accounting, there are zero explicit hax-opaque functions in the isolated core. The current source has one reviewed `hax_lib::fstar::before("noeq")` directive, on `InitialRatchetChains`, to avoid requiring decidable equality for the abstract `RatchetChain` imported through the PQXDH-facing interface. Removing executor-bearing concrete records eliminated the three former executor-related `noeq` directives. The remaining directive leaves propositional equality available and does not hide implementation fields from explicitly friended proof modules. Exactly two generated `friend` directives let the root Ratchet implementation inspect `Ratchet.Refined` and let `Ratchet.Refined` inspect `Ratchet.Control` while their public interfaces keep representations abstract. Every repository-owned function outside the core is opaque to extraction and is covered by the recursively derived, whole-file `adapter-rust` manifest. OR-01--OR-17 name the proof-relevant functions and complete impl blocks within the claimed trace; OR-18 exhaustively classifies every other function by its whole-file review unit. Changing any of them still trips the conservative file-level review baseline.

## Assumed primitive laws

The F* modules do not declare these as local axioms. Their honest-run results are conditional on adapter inputs satisfying the relevant relations. The ProVerif model represents the same boundary with ideal symbolic constructors and the eight reductions in `crypto.pvl`, which is stronger than a computational claim and is stated explicitly here.

| ID | Reviewed law |
| --- | --- |
| PL-01 | Key-generation coins used for distinct honest identities, one-time keys, ephemeral keys, KEM encapsulations, and sessions are fresh. Seeded identity generation is deterministic for the supplied seed. RNG implementation security is not proved. |
| PL-02 | An honestly generated Ed25519 signature verifies to the exact signed message under the matching public key; successful verification used as authentication has the usual symbolic unforgeability/provenance meaning. No equation verifies a signature under a different key or for a different message. |
| PL-03 | Ed25519 public- and secret-key conversion name the same X25519 identity secret for the honest pair. Invalid encodings/conversions are rejected by the adapter. |
| PL-04 | Each complementary X25519 pair computes the same DH value. All-zero outputs are rejected by the core before root construction. General X25519 arithmetic and contributory behavior are not proved. |
| PL-05 | ML-KEM encapsulation and decapsulation with the matching public/secret key pair and ciphertext produce the same shared secret. The production ProVerif term `mlkem_shared_secret(public_key, coins)` additionally assumes public-key binding, equivalently strong shared-secret collision resistance across distinct public keys in the symbolic theory. Generic IND-CCA does not imply this stronger equality property, and neither the ML-KEM implementation nor a computational reduction for this assumption is proved. The production root input and associated data omit the ML-KEM public key and ciphertext, so the modeled re-encapsulation resistance also relies on AR-13's one-bundle-per-fresh-identity lifecycle. |
| PL-06 | HKDF is deterministic for identical input and label. Production uses no-salt HKDF-SHA-512 with the exact 46-byte `PQXDH_INFO` domain for the first 32-byte root projection and the exact distinct 41-byte `SYM_RATCHET_INFO` domain for both initial and per-record expansion. ProVerif uses one public ideal stream indexed by input and exactly those two symbolic domains, with shared first-32, second-32, and final-12 projections; equal symmetric inputs therefore make initial-left equal step-key and initial-right equal step-next-chain, and output length supplies no extra separation. The extraction-backed root-input replacement carries explicit `FF^32` before DH1, DH2, DH3, DH4, and the ML-KEM secret. The core privately constructs every concrete symmetric request as `{ input, SYM_RATCHET_INFO }`, and the adapter must return the correct typed response to that exact request. The HB-50 interface/test synchronizes the exact labels, no-salt uses, 32/64/76-byte outputs and splits, shared-domain prefix, padding, contribution order, and root length with the compiled core and reviewed adapter wiring. Current structural Lean equations prove the 64-byte initial split, beacon/server role offsets, and the 76-byte per-step `0..32` key/`32..64` next-chain/`64..76` nonce split. `ResponseRefines` supplies the explicit faithful-response law, and the checked send resume theorem uses it. Role complementarity as opposing `KernelRefines` states, future canonical phase reachability/publication, and paired-session preservation remain open. Distinct-position key/nonce non-reuse additionally assumes noncollision and the affine, establishment, trusted-restore, and external one-owner conditions in AR-03, AR-05, AR-06, and AR-18. The synchronization is not an HKDF security or semantic-refinement theorem, and concrete primitive semantics/totality, response provenance, projected-output noncollision, compiler correspondence, adapter panic behavior, and physical erasure remain obligations rather than HKDF laws. |
| PL-07 | Production ChaCha20-Poly1305-IETF receives exactly a 32-byte key, 12-byte nonce, the 153-byte associated data, and plaintext, and returns ciphertext plus a detached 16-byte tag; it receives no AEAD label, sequence, or sender ID. For a fixed key, nonce, associated data, and ciphertext/tag, decryption is deterministic and inverts an honest seal. Authenticity under the intended honest key/context supplies the record-provenance assumption, but does not forbid the same ciphertext/tag from opening under another adversarially selected key/context. The ordinary symbolic record rule is stronger and exact-opening; the separate weak-AEAD negative control deliberately permits two distinct openings so that rule is not used as evidence for CTX's added benefit. |
| PL-08 | ProVerif represents production CTX without a domain label as `blake2b512(ctx_preimage(key, nonce, associated_data, retained_aead_tag, sequence_le64(sequence), sender_id_le64(sender_id)))`. Its six-field `ctx_preimage` data constructor idealizes the injective fixed-width serialization, while the free `blake2b512` constructor idealizes one-way collision-free hashing; neither symbolic constructor proves the concrete BLAKE2b implementation. F* proves that the extracted fixed-size CTX input is injective over all six fields and that, for arbitrary pure 229-to-64-byte hash and AEAD-open functions, two semantically distinct accepted explanations of one fixed `C || T || U` payload return an explicit witness containing unequal production transcripts with equal hash outputs. Independently, Lean proves the corresponding witness for the ideal `openRecord` parser/verifier, a factor-one probability bound from that ideal-model misattribution event to VCVio collision-resistance advantage, equality between the current generated 229-byte builder and the ideal preimage, and component-level Boolean privacy preservation relative to modified nonce-AEAD IND$ and a classical public ROM. The AEAD-open function may multi-open across unequal keys or contexts; only same-input determinism is used. SSProve gives a finite hidden-ROM adversary and two-query verifier, proves the same-run collision extractor with a `q + 2` trace bound, and separately proves a uniform programming representation plus mismatch-to-secret-query privacy hop over one-bit semantic atoms. BLAKE2b collision resistance, concrete ChaCha20-Poly1305 security, production-width and cross-prover representation bridges, numerical collision and hidden-query bounds, protocol-level multi-key privacy/integrity composition, PPT/runtime accounting, the adapter's BLAKE2b call, field provenance, and compiler correspondence remain assumptions or unproved obligations. ProVerif separately checks one explicit CTX/no-CTX weak-AEAD differential control and six single-field controls. |
| PL-09 | The generated ProVerif associated-data reduction produces `beaconcrypt_associated_data(tag_ed25519(server_identity), tag_ed25519(beacon_identity), pqxdh_domain(), symmetric_ratchet_domain())`, so the server identity is first and the exact same two symbolic HKDF-domain values form the suffix. The `tag_ed25519`, `tag_mlkem768`, `tag_x25519_prekey`, and `tag_x25519_one_time` constructors idealize disjoint authenticated encodings; F* separately checks the concrete `0x01`, `0x03`, `0x04 || 0x80`, and `0x04 || 0x81` markers, key-role enforcement, associated-data order, and 153-byte length. HB-50 synchronizes those markers, roles, exact field order, offsets, length, and shared-domain use with the compiled core and ProVerif interface. The strong ProVerif controls reject ML-KEM/X25519 algorithm confusion and X25519 prekey/one-time role confusion, while deliberately weakened encoding theories make both confusion witnesses reachable. The synchronization does not turn the injective symbolic constructors into a primitive, parser, compiler, or computational theorem. |
| PL-10 | ProVerif's fresh assigned-key-ID names and constructor-generated `first_sequence`/`next_sequence` terms model only checked, non-exhausted, collision-free bounded prefixes of the production `u64` spaces. The state-neutral receive process explicitly unrolls one sequence-3 rejection/success leg and one exact capacity-50 success/release leg but does not prove arbitrary gaps, cache arrangements, finite-counter executions, or wraparound behavior; those control facts are covered by F*. |
| PL-11 | For equal-length byte slices, production `libsodium_rs::utils::memcmp` returns true exactly when every byte is equal. Its constant-time implementation is not proved. The receive trace relies on this law for the computed-versus-wire CTX commitment; the other repository use additionally compares `SecretArr` system/role `TypeId`s and is not reached by the claimed trace. |
| PL-12 | The unkeyed 32-byte BLAKE2b `crypto_generichash` digest is a deterministic, collision-resistant identity for the exact version-2 envelope bytes used in `SnapshotHead` comparison and parent linking. It provides neither authenticity, integrity, provenance, confidentiality, nor freshness; those properties belong to the `SnapshotStore` contract in AR-18. F* and ProVerif do not model this call or its computational collision bound. |
| PL-13 | The SSProve PQXDH-and-ratchet confidentiality games treat authentication, ML-KEM, root derivation, symmetric KDF, and record encryption as ideal finite operations. The root input preserves the four-DH-plus-KEM order, the Ed25519 seed and converted X25519 identity are compromised jointly, honest and substituted root queries use distinct table entries, and one shared symmetric table enforces initial/step prefix consistency. The closed active-classical capstone proves exact zero advantage for statically selected forwarding and for replacement tampering under the fixed intended verification key; it does not model a self-consistent attacker-owned identity and signed bundle, which production may accept because the advertised identity key is unsigned. The closed passive-classical and passive-quantum classical-query forwarding games also have zero advantage. The attacker-facing bounded-ROM hop conservatively fixes the four DH atoms, hides only the honest ML-KEM contribution and tagged table, gives the observer the challenge ciphertext and adaptive root/symmetric queries, and bounds the modeled active-classical actions plus both positive passive forwarding cases by the probability of querying the exact hidden ratchet-pad input. No numerical production-width bad-query bound is proved. Active quantum replacement recomputes from the exact accepted fixed substituted input and has advantage one within that capability model. The abstract confidentiality record is sequence zero and selects the second initial half; it is not production's first server response. Arbitrary interactive network schedules, a general registration transcript, record-tampering/decryption interfaces, randomized interactive distinguishers, multiple sessions, replay, compromise, forward secrecy, CTX composition, QPT adversaries, and QROM access remain outside the results. |

## Production bindings and trace preconditions

These statements connect opaque primitive results and the surrounding production trace to the extracted kernel. The concrete ratchet owns control, direct chains, tagged material slots, exact requests, fixed response partitions, private staged receive state, and explicit commit/rollback phase transitions. No executor or callback crosses the core boundary. The current default core translates to Lean, its generated control driver refines the ideal receive step directly at the common 50-skipped-key bound, and checked effect refinement covers the precise kernel representation, an assumed response law, non-exhausted send begin/resume/ideal success plus cancellation, rollback for supplied finite failed-trace witnesses, conditional open-reply consistency, generated cached-open construction, the control-plane cached-consumption result, and conditional cached success; structural equations separately establish send exhaustion and advanced-state return for either seal result. The unproved future-receive, cached-material-array-publication, initial-composition, restoration, ideal-send-exhaustion, and adapter-interpreter connections remain assumptions. Affine safe-Rust ownership, establishment-gated application state, inert update snapshots, canonical duplicate-rejecting serde, trusted-store payload integrity/provenance, monotonic generation/head CAS, and loser fencing are maintained adapter or deployment refinements. Concrete primitive semantics and output noncollision, effect-driver fidelity, parser/codec/digest fidelity, store integrity/provenance/atomicity/durability/rollback resistance, peer selection, compiler behavior, panics, crash behavior, and deployment coordination remain preconditions. Snapshots have no cryptographic authentication or encryption.

| ID | Required refinement or precondition |
| --- | --- |
| AR-01 | Production stores one `ConcreteRatchetKernel` as its authoritative in-memory ratchet rather than maintaining parallel logical control, role-typed chains, or material arrays. Its private refined kernel contains direct `RatchetChain` values and only tagged `CachedReceiveKey<RatchetMaterial>` slots; no executor is stored. The kernel and effect continuations are non-`Clone`, and private fields prevent adapter construction or retagging. Checked Lean `KernelRefines` imports the extracted receive-control refinement, equates concrete and ideal send sequence plus both direct chains, requires the send counter below `2^64`, relates live cache slots bidirectionally to ideal skipped keys, and requires every slot above the live prefix to be empty. Send continuations and cached opens have additional checked relations; `begin_receive_cached_refines` derives the latter from an actual generated cached `begin_receive` result when the ideal skipped-key lookup is supplied. Establishing the future staged invariant, completing cached material-array publication, proving future publication, and proving restoration remain open; restoration also remains conditional on AR-06 canonical provenance. |
| AR-02 | For every initial or later symmetric derivation, the core privately constructs `SymmetricRatchetKdfRequest` with the exact root or current chain as `input` and exact `SYM_RATCHET_INFO` as `info`; the adapter cannot choose the request, partition, direction, sequence, or cache slot. It may only answer `InitialRatchetKdfPending` with a typed 64-byte response or a ratchet KDF phase with a typed 76-byte response. The core performs all splitting. Checked structural Lean equations cover both partitions and role offsets. Checked `ResponseRefines` states the exact ratchet-step law equating a parsed response to `cr.kdfChain` and `cr.kdfMsg`, and the send resume theorem uses that law; future receive induction and initial role composition remain open. The adapter must execute each emitted request exactly once, return its result only to the owning continuation, and not retain or replace state. Concrete HKDF semantics/totality, projected-output noncollision, response provenance, side effects or panics, timing, rate limiting, denial-of-service resistance, crash behavior, physical erasure, and compiler correspondence remain external. |
| AR-03 | `begin_send` either returns exact entry state on exhaustion or an affine `SendKdf`; pre-response cancellation returns the entry state. `SendKdf::resume` privately partitions the response, advances chain/counter, and returns `SendSeal` with exact material, sequence, and unchanged opaque context. `SendSeal::finish` consumes the capability and returns the advanced kernel on both seal success and failure, preserving the protocol's failed-send key consumption. Lean structurally proves exhaustion and either finish result, and under `KernelRefines` plus `ResponseRefines` proves non-exhausted begin, resume, ideal encrypted success, and cancellation with the advanced post-send kernel related to the ideal state. The synchronous adapter must still drive this sequence once and reinstall the returned kernel; that driver and the concrete AEAD/commitment/serialization interpretation are not proved. Operational core types and `RatchetManager` remain affine and update methods return inert snapshots, while durable one-owner behavior additionally requires AR-18. Effect-driver fidelity, projected-output noncollision, panic/crash behavior, physical erasure, unsafe/FFI copies, and compiler correspondence remain external. |
| AR-04 | `begin_receive` performs admission and lookup without changing the entry kernel. Each `ReceiveKdf::resume` extends only private working control and staged skipped material; `ReceiveOpen` exposes the exact selected material/sequence/context. Admission counts only retained skipped keys against the 50-slot cache, while the target is derived and consumed separately without occupying a slot. Checked Lean proves KDF cancellation, open rejection, and `ReceiveOpen::finish(None)` return the exact entry kernel and preserve `KernelRefines`; `ReceiveFailureTrace` extends that result to any supplied finite failed-trace witness, but no theorem yet constructs such a witness from every `begin_receive` execution. `OpenReplyRefines` conditionally relates the supplied result to the phase-selected material and `cr.dec`. Given `KernelRefines`, target/index equality, and an ideal skipped-key lookup, `begin_receive_cached_refines` proves that generated `begin_receive` returns a cached open satisfying `CachedOpenRefines`. Checked theorems then prove exact cached material and ideal success against cached `recvStep`; concrete successful finish returns the same plaintext under the generated publication equation. `finish_receive_with_removal_consumed_refines` proves that the consumed control state refines ideal skipped-key filtering, while full post-state refinement remains conditional on `CachedPublicationRefines` because the material-array swap and empty suffix are not yet proved. Future KDF staging/publication and the adapter driver remain open. A failed maximum-gap future frame may repeat up to 51 KDF calls without live cache growth in production. Concrete CTX/AEAD semantics, ephemeral conversion, driver panics/side effects, crash/concurrency atomicity, authoritative-state copies, projected-output noncollision, rollback, and physical erasure remain external. |
| AR-05 | High-level Server lookup selects one unique `EstablishedRemote` entry matching the requested or parsed-and-rechecked sender ID and leaves non-selected peers unchanged; Beacon messaging succeeds only from `BeaconState::Established`, whose variant owns the sole operational ratchet and retained server binding. Production constructors for established peers are crate-private and used by PQXDH commit or fresh restoration from the trusted store. Manual insertion/deletion/reset, associated-data mutation, and mutable-ratchet access are test-only or crate-private. This is establishment-gated runtime state, not a public generic compile-time typestate witness or a whole-map F* theorem. |
| AR-06 | The internal payload requires the exact five-field ratchet schema, rejects unknown and legacy `send_past` fields, validates type/role arrays and receive bounds, sorts output, and rebuilds only through `start_concrete_restore`/`concrete_restore_receive_key`/`finish_concrete_restore`. The custom numeric-map visitor observes entries before `HashMap` insertion, accepts only canonical decimal `u64` spellings, and rejects duplicate parsed keys; `HashMap` itself is not evidence of raw input uniqueness. The extracted builder proves structural ordering, capacity, tag/material alignment, and atomic append but cannot prove canonical HKDF provenance. Version-2 compatibility and the 50-entry capacity are unchanged, and restoration continues to admit a structurally valid full cache, including trusted snapshots written under the former failure-retention lifecycle. A full restored cache can process the next no-skip target because that target is consumed separately, but it cannot retain another skipped key until an authentic cached frame frees a slot. The enclosing `ServerSnapshot` requires byte-identical decode/re-encode before AR-18 activation, while the trusted store must supply the exact payload bytes with their integrity and provenance protected. `reachable_restore` remains conditional on canonical imported chains/materials, and F* does not prove serde, canonicality, digest behavior, store integrity/provenance, or snapshot provenance. |
| AR-07 | Cap'n Proto Phase 1 fields translate exactly to the core's tagged identity, role-tagged prekey, role-tagged one-time key, and typed PQ key. Signature verification authenticates the bytes passed to `validate_init_kex`; field position alone is never trusted. |
| AR-08 | Beacon/server DH1--DH4 and ML-KEM results are paired in the order represented by `PqxdhSharedSecrets`, and production passes the exact core-built root transcript plus `PQXDH_INFO` to deterministic HKDF. The F* common-root bridge starts from an `AuthenticatedBeaconRegistration` and corresponding `PendingServerRegistration`, assumes their verified `RootKeyInput` transcripts are equal, and proves that applying one fixed pure 32-byte root derivation yields equal roots. It does not prove the assumed transcript equality, the adapter's authentication or transcript provenance, or concrete PQXDH HKDF semantics. |
| AR-09 | Role-bound candidate start functions construct `InitialRatchetKdfPending` with the fixed `{ root, SYM_RATCHET_INFO }` request and complementary role continuation; `resume_initial_ratchet_kdf` accepts only the typed 64-byte response, splits it into two 32-byte halves, orders them, and returns a direct-chain `ConcreteRatchetKernel`. The current source stores no ratchet executor. Checked structural Lean equations establish the initial request/accessor, response split, and beacon/server role offsets. The predecessor F* capstone proves complementary role kernels, all-sequence opposing material equality, and paired-session preservation for the old executor/callback implementation; current Lean still must compose the structural initial results into opposing `KernelRefines` states and connect them to subsequent send/receive phases under explicit KDF and adapter laws. Concrete HKDF semantics/totality, transcript equality or authentication provenance beyond AR-08, projected-output noncollision, and compiler correspondence remain external. |
| AR-10 | Beacon construction maps the authentic compiled-in public key and numeric server identity-key ID to the exact `ServerBinding` stored by `BeaconFresh`; F* proves preservation through establishment and derives agreement from successful response-key and sender-ID checks. The Phase-2 identity, authenticated sender ID, and assigned-ID bytes supplied to the core must be the parsed/authenticated production values. The adapter stores the public key separately before establishment and publishes the ratchet only in `BeaconState::Established`; initial trust-anchor authenticity, wire/open provenance, and runtime variant selection remain adapter assumptions. |
| AR-11 | `RegistrationStatus::Fresh` means the exact 64-byte semantic registration ID is absent from the server set, and successful acceptance inserts it before later response failure. Across persistence, monotonicity requires all owners to use the same AR-18 store; the F* classification and ProVerif one-owner process do not prove the real set, snapshot, or replicas. |
| AR-12 | `KeyIdAvailability::Available` means the exact checked next ID is absent from the Server's `EstablishedRemote` map. Recoverable response failures precede the in-memory peer/counter commit, while replay consumption intentionally occurs earlier. Persistent crash/concurrency publication and result release are refined by AR-18, not proved by the pure transition theorem. |
| AR-13 | `Beacon::new` creates one fresh Ed25519 identity, X25519 prekey, and ML-KEM keypair together. A fresh Beacon emits one registration bundle, entropy/key material supplied as explicit core coins is fresh, and its `BeaconState` advances to `InitSent` or terminal abort without reusing a bundle. The supported API exposes no operation that rotates the ML-KEM key while retaining that identity; constructing a new Beacon creates another fresh identity and keypair. This lifecycle is reviewed adapter behavior, not a ProVerif or computational theorem. |
| AR-14 | The claimed production trace starts through successfully initialized or validated high-level state, permits record processing only from the establishment gate in AR-05, and uses `PersistentServer` with a conforming external store plus AR-18 whenever server state must survive or coordinate owners. C, Go, and Python binding checkpoint import/export is a documented single-owner convenience path and is outside the durable no-fork/no-rollback trace because its in-memory store cannot fence independent imports or ensure that the caller persisted an export before using an accepted or otherwise state-changing output. A normally rejected receive leaves the binding checkpoint byte-for-byte unchanged and requires no new export. Cap'n Proto, serde, allocation, FFI/bindings, snapshot codec/digest behavior, persistence media integrity/provenance, store behavior, zeroization, and replicas are reviewed adapter or deployment components, not silently promoted to verified components. Direct beaconcrypt-core construction and test-only compatibility/mutation paths remain outside the trace. |
| AR-15 | Phase-2 `identityKey`, `ephemeralKey`, `kemCipherText`, `appCipherText`, and `keyId` encode respectively the candidate server identity, ephemeral X25519 public key, ML-KEM ciphertext, complete initial `CryptoFrame`, and proposed remote ID. The nested initial `CryptoFrame.keyId` is the candidate server identity-key ID. The beacon parses those same fields, compares `identityKey` with its retained public key, binds the public material into its DH/KEM/AD/decryption path, opens the frame against its retained numeric server ID, and accepts only when the authenticated frame sender and decrypted eight-byte assigned-ID prefix match the retained binding and outer `keyId` respectively. |
| AR-16 | A production `CryptoFrame` encodes `seq = key_seq`, `keyId = sender_kid`, and `cipherText = AEAD ciphertext || 16-byte tag || 64-byte CTX commitment`; the local `Encrypted.key_id = target_kid` is not placed on the wire. Server receive treats the parsed sender ID as a lookup hint for its peer map and rechecks it in the selected helper; Beacon receive selects its sole server and passes the constructor-bound server ID as the expected sender. Both reject payload lengths no greater than the exact overhead before admitting the parsed sequence. The final receive phase selects the target key/nonce without live publication and carries the original 153-byte associated data, parsed ciphertext, and sender ID as opaque context. The adapter passes the phase's material, sequence, associated data, parsed 16-byte tag, and sender ID to the F*-proved transcript helper, hashes the returned 229 bytes, and performs AEAD open. A `None` result returns the entry kernel, while `Some` publishes and consumes the same selected target. ProVerif's symbolic forged frame starts after parser/sender/length gates and does not prove their byte-level behavior; Lean does not yet prove the adapter context plumbing. |
| AR-17 | The five baseline honest-task canaries and six independent state-neutral receive canaries are routed only to their intended honest symbolic sessions. The private `honest_origin`/`malicious_origin` tables and split server processes classify proof-model recipients; they are not production authorization checks and do not prove the surrounding C2's task-selection policy. Attacker-owned sessions receive only `MALICIOUS_TASK_SECRET`, whose deliberate disclosure is a non-vacuity control. |
| AR-18 | `PersistentServer::create` activates generation zero only after empty-store CAS. `restore` loads the current exact bytes from the trusted store, validates and canonically decodes them, encodes the same state at generation `g + 1` with the loaded digest as parent, and becomes live only after CAS from the complete loaded head. Accepted receives and other state-changing operations encode the complete successor at `g + 1`, withhold application output until CAS succeeds, and poison the owner after CAS or successor-construction failure so it cannot continue as a fork. A rejected receive clears the precautionary poison flag and returns without next-generation arithmetic, encoding, digesting, loading, or CAS; its server, local head, authoritative snapshot, generation, and store call count remain unchanged. This may delay stale-owner discovery until a later state-changing operation, which must still win normal CAS before output release. Post-commit receive-update rendering can return `OutputEncoding` without rolling back the accepted state or poisoning the owner. `SnapshotStore::load` must return only protected exact snapshot bytes accepted for that lineage, thereby supplying their integrity and provenance. CAS must atomically compare the complete head, durably install the exact replacement bytes and new head before success, share one authoritative lineage across owners, and keep its head rollback-resistant. The unkeyed digest is only CAS identity and parent-link data. Snapshots have no cryptographic authentication or encryption, and neither F* nor ProVerif proves this contract, the store, crash recovery, or deployment discipline. |

## Proof-library and tool assumptions

| ID | Reviewed trusted surface |
| --- | --- |
| LA-01 | hax correctly translates selected Rust semantics to generated F* and ProVerif syntax, and hax/Charon/Aeneas correctly translate the no-exclusion default core to generated Lean. Exact tool revisions are pinned, but translation and compiler correctness are trusted. Successful Lean translation or `lake build` is coverage/typechecking evidence, not a behavioral refinement theorem. |
| LA-02 | PQXDH's and the commitment builder's exact slice proofs use the val-only contracts for `Rust_primitives.Hax.Monomorphized_update_at.{update_at_range,update_at_range_to,update_at_range_from}` from the pinned hax proof libraries. Their reviewed contracts replace precisely the selected range and preserve its prefix/suffix. |
| LA-03 | The PQXDH key-ID and commitment LE64 proofs call val-only `Rust_primitives.Integers.shift_right_lemma` for shifts 8 through 56. Its exact contract is `Lemma (v (shift_right a b) == v a / pow2 (v b))` with an SMT pattern for the shift; the underlying pinned `shift_right` is `opaque_to_smt`. The commitment decoder additionally calls the pinned val-only `pow2_values` contracts for exponents 8, 16, 24, 32, 40, 48, 56, and 64. |
| LA-04 | In the predecessor F* artifact, `update_at_usize`, `repeat`, `array_of_list`, `Core_models.Option.impl__take`, fixed-range slicing, slice copying, Option/Result, selected finite integer arithmetic/casts, equality, and `Core_models.Num.impl_u64__MAX` are transparent locked-library definitions. Its refined extraction uses transparent cached-record equality, single-index update, and `Option::take` for whole-entry removal/publication; its three executor-related `noeq` directives are historical generated exceptions. The current source retains only the `InitialRatchetChains` `noeq` directive. The commitment proof also uses pinned checked division and sequence extensionality/equality lemmas. These facts do not transfer predecessor concrete theorems to the current phase definitions. |
| LA-05 | F*, Z3, and their kernel/runtime correctly check the generated modules and handwritten lemmas under the locked flags. There is no local `assume`, `admit`, lax mode, or admitted-query flag. |
| LA-06 | ProVerif 2.05 soundly analyzes the stated symbolic model and the result classifier accurately enforces the complete expected result set. This is not a computational reduction for the production primitives. |
| LA-07 | The handwritten Lean ideal symmetric ratchet expresses the intended semantics, and each future refinement theorem must state a sufficiently strong relation rather than merely prove generated definitions type-correct. External effect laws must bind a response to the exact request and bind seal/open results to the exact material, sequence, and context. The proof still trusts Lean's kernel/runtime, the pinned libraries, Charon/Aeneas semantics, and correspondence from compiled Rust to extracted Lean. |
| LA-08 | Rocq 9.0.0, SSProve 0.2.4, MathComp 2.4.0, and `coqchk` correctly check the exact-label, bounded-ROM, CTX, protocol-game, and single-run state-separation suite. The structural exact-label and pure state-separation capstones, including the optional public-view projection and rejection facts, are closed under the global context; the probabilistic modules' exact assumption allowlist contains propositional extensionality, dependent functional extensionality, constructive indefinite description, and the abstract probability carrier `R : realType`. Repository admissions, SSProve's unaccepted free-program interchange theorem, and hax's unsafe aggregate prelude are not dependencies of the reported capstones. The joint sampler and typed package constants are pinned structurally but are not reported as contextual theorems because the pinned package layer exposes rejected assumptions through those stock bridges. Direct hax-to-SSProve extraction remains rejected until its prelude and emitted array operations pass the same gate. |
| LA-09 | The VCVio computational modules trust pinned commit `cbd4144b51d92da00dd50f05e068b2348fa6e529` and its manifest-locked PolyFun and loom2 dependencies to implement the stated `ProbComp`, event/output probability, collision-resistance experiment/advantage, lazy random oracle, and state-handler semantics. Lean's kernel checks the imported theorem instances, and the recorded `#print axioms` closures contain only standard Lean classical/quotient axioms, but model adequacy is a library assumption. The wider pinned dependency tree contains unrelated admitted modules, so any production claim additionally requires a transitive import, admission, `unsafe`, and option audit. |
| LA-10 | The handwritten `Pqxdh.Crypto` interface and ideal registration/record transitions express the intended cryptographic semantics only under their stated correctness and output-length laws. The CTX reduction additionally treats collision resistance of the supplied pure `c.blake2b` as the target hardness assumption; it does not prove that assumption, transformed-AEAD privacy or integrity, PPT preservation, or equality with the adapter BLAKE2b call. The generated transcript builder-to-ideal-preimage equality discharges that portion of the earlier representation gap, subject to the remaining extraction and compiler correspondence obligations. |

## Generated-code exceptions

Generated directories are regenerated and never hand-edited. Their exact file
sets and hashes are in the manifest, while the following exceptions explain
constructs that are accepted rather than silently treated as proved
production behavior.

| ID | Reviewed exception |
| --- | --- |
| GE-01 | The five tracked F* implementation files are generated from the predecessor executor/callback snapshot. They expose private Rust constructors/fields, including Control transitions, Refined cached/pending values, request input/info, the old `ConcreteRatchetChain` executor pair, concrete kernel/restore records, fixed material records, output partitions, and initial directional chains. Their four generated `noeq` directives and concrete friend-based lemmas therefore describe that snapshot, not current Rust; the current source has only the `InitialRatchetChains` `noeq` directive. These files remain reviewable historical proof artifacts until regenerated/ported, but no current production claim may rely on their executor preservation or callback lifecycle theorems. The authenticated-root bridge still calls no concrete authenticator or HKDF and remains conditional on equal verified transcripts plus one fixed pure derivation. |
| GE-02 | Generated F* calls the val-only range-update contracts in LA-02; the handwritten key-ID and commitment proofs call the val-only shift lemma in LA-03, and the commitment decoder calls the listed val-only power-of-two contracts. `while_loop_return`, the opaque `u64::to_le_bytes` model, a derived whole-`ServerBinding` equality assumption, local `assume`, and local `admit` are not accepted exceptions and are rejected by policy/fingerprint drift. |
| GE-03 | `pro-verif/extraction/lib.pvl` contains hax's generated preamble and selected-type plumbing: the public channel `c`; `Option`, `Some`, `None`, and `Option_err`; the `empty` constant; bitstring, nat, and bool defaults; bitstring/nat errors; `nat_to_bitstring`; six selected nominal record types; twelve type converters; six arbitrary record default constants; and six record error helpers. There are nine error helpers total and eleven textual `construct_fail()` occurrences (declaration, reduction, and nine error paths). These are generated backend plumbing, not protocol constructors. |
| GE-04 | Handwritten ProVerif never calls a generated `from_bitstring`, default, or error helper. The only generated converter it calls is `RootKeyInput_to_bitstring`, exactly four times, to feed the ideal root constructor in the honest beacon, honest server, malicious-registration server, and bounded active-quantum witness paths; every other converter is unused by the handwritten model. The gate enforces this allowlist. |
| GE-05 | Exactly three Rust `hax_lib::proverif::replace` fragments are permitted: `registration_id` projects identity plus one-time key; `build_root_key_input` preserves explicit `FF^32`, DH1, DH2, DH3, DH4, KEM order; and `build_associated_data` reduces to the server-first tagged identities followed by the same PQXDH and symmetric-ratchet domain representatives used by HKDF. A fourth replacement fails the gate. HB-50 synchronizes the covered root-input and associated-data structures with the compiled core and adapter contract; this does not make the handwritten replacement an extracted equality or refinement theorem. |
| GE-06 | The ProVerif `build_root_key_input` replacement models its successful constructor path and omits Rust's all-zero-DH error. Modeled server/beacon processes invoke it after their ideal DH computations; F* separately proves exact zero-DH rejection. No ProVerif result is claimed for malformed core calls outside those processes. |
| GE-07 | The generated library's public constructors/accessors and arbitrary type converters are more permissive than Rust privacy. Modeled role processes construct private core values directly and never accept attacker-provided converter/default/error values. Agreement events carry values parsed from the public wire, not private shadow transcripts; their complete constructor equality remains a handwritten symbolic boundary rather than byte-level parsing or extraction linkage. |

The structural baseline for the generated ProVerif file is six selected types, twelve converters, six record default constants, twenty-two constructors/functions, twenty reductions total, and the three required production-backed operations. The generated F* file set is exactly `Beaconcrypt_core.Commitment`, `Beaconcrypt_core.Pqxdh`, `Beaconcrypt_core.Ratchet`, `Beaconcrypt_core.Ratchet.Control`, and `Beaconcrypt_core.Ratchet.Refined`.

## Handwritten backend fragments

The review atom is the entire listed file unless a smaller embedded fragment is
named. Consequently every declaration, equation, process, event, query, and
proof helper outside a generated directory belongs to exactly one entry below;
whole-file fingerprints force renewed review even when a structural count is
unchanged.

| ID | Handwritten review unit | Contents and status |
| --- | --- | --- |
| HB-01 | `proofs/fstar/Beaconcrypt_core.Ratchet.Lemmas.fst` and `.fsti` | Checked predecessor proof, not an axiom file and not a proof of current Rust. It establishes exact request construction, executor application/preservation, output partitioning, control/cache/replay/restoration facts, canonical reachability, private preparation, callback-failure equality, conditional callback-success publication, retry/capacity/max-gap results, and public seal/open preservation for the executor/callback source snapshot from which its generated modules were produced. Its generic relations and proof structure are reusable design evidence, but every concrete theorem mentioning executor-bearing chains, `concrete_seal_next`, or `concrete_open_and_finish` must be ported to current initial/send/receive phase definitions before supporting production claims. It proves no concrete KDF semantics, response provenance, projected-output noncollision, adapter-driver fidelity, or compiler correspondence. |
| HB-02 | `proofs/fstar/Beaconcrypt_core.Pqxdh.Lemmas.fst` and `.fsti` | Checked predecessor proof, not an axiom file. Its type/role layout, transcript, AD, binding, allocation, and conditional common-root results remain evidence for corresponding unchanged generated definitions. Its role-bound concrete-kernel, fixed-executor directional equality, paired reachability, and callback seal/open composition theorems describe the predecessor concrete API and do not prove the complete current lifecycle. Lean now checks `InitialRatchetKdfPending` request/accessor, response splitting, and role offsets plus the ratchet slices recorded in HB-28/HB-29; initial opposing-kernel composition, future receive publication, and end-to-end paired-session preservation still require a phase-aware Lean or regenerated F* port. It does not prove concrete HKDF semantics, response provenance, adapter interpretation, or its root-transcript equality/authentication premise. |
| HB-03 | `proofs/pro-verif/crypto.pvl` | Six abstract sorts, 35 constructors/functions, eight primitive reductions, and eight helpers. The KDF portion is one shared no-salt HKDF stream with exactly two production-domain representatives and three common projections; stored record material retains only its key and nonce projections. The authenticated-context portion has one server-first four-field associated-data constructor, four disjoint key type/role tags, exact four-input AEAD constructors, explicit sequence and sender-ID LE64 encodings, a six-field CTX preimage, and one ideal BLAKE2b-512 constructor without AEAD or CTX labels. Its free two-argument `mlkem_shared_secret` constructor carries PL-05's explicit public-key-binding assumption rather than deriving that property from generic IND-CCA. This is the trusted symbolic primitive theory described by PL-02--PL-11. |
| HB-04 | `proofs/pro-verif/environment.pvl` | Four local types, 14 constructors/functions, 26 events, and twelve named processes covering the wire, explicit receive state/cache, replay, roles, malicious registration, state-neutral receive, private state, and compromise. The injective `establishment_transcript_t` constructor carries exactly 18 ordered security-relevant values: server identity, beacon identity, authenticated `InitKex`, registration ID, prekey, one-time X25519 key, selected ML-KEM public key, server ephemeral key, exact KEM ciphertext, initial frame, complete response, ordered root input, derived root, exact associated data, assigned beacon key ID, pinned server key ID, session identifier, and registration origin. A separate authenticated-bundle constructor binds the three individually selected public keys to the signed initiation before response commitment. The file also includes two private origin tables, private ordinary and receive snapshot channels, the bounded honest record prefix, an attacker-usable malicious registration response, a short repeated-rejection/success/delayed-delivery leg, and the exact maximum-gap 50-skipped-key capacity leg. |
| HB-05 | `proofs/pro-verif/queries.pvl` | Five baseline secrecy queries and seven injective correspondences; all twelve must be true. The additional bundle correspondence compares the authenticated initiation with the server's explicit prekey, one-time X25519 key, and selected ML-KEM public key, while beacon/server commitment compares one complete 18-field establishment term so equal shared-secret or root terms alone cannot satisfy agreement. |
| HB-06 | `proofs/pro-verif/reachability-queries.pvl` | Eight non-vacuity queries whose negations must be false: the five original honest acceptance, replay rejection, abort, exact 18-field commit, and record-receive traces; the added authenticated-bundle acceptance event; malicious-registration response commit; and attacker derivation of the malicious-session canary. |
| HB-07 | `proofs/pro-verif/compromise-queries.pvl` | Two required positive deleted-key secrecy results and three required negative cached/live-chain secrecy results. |
| HB-08 | `proofs/pro-verif/baseline.pv` | Top-level replicated baseline process containing concurrent honest sessions and attacker-owned registrations. |
| HB-09 | `proofs/pro-verif/compromise.pv` | Top-level replicated synchronized late-compromise process for the honest-session snapshot; malicious registrations are exercised by the baseline isolation scenario instead. |
| HB-10 | Three `hax_lib::proverif::replace` attributes in `../beaconcrypt/src/pqxdh.rs` | Embedded handwritten backend fragments described by GE-05 and GE-06. The associated-data replacement is now a reduction into the exact server-first symbolic structure and reuses the HKDF domain representatives rather than introducing an opaque unrelated constructor. The fragments are feature-gated and have no ordinary Rust runtime effect. |
| HB-11 | `proofs/pro-verif/check-results.awk` | Trusted result-control logic for the CTX/no-CTX differential control and six independent CTX field controls, the twelve-query baseline including authenticated-bundle and complete establishment-transcript agreement, four explicit attacker modalities including the eight-value active-quantum recovery witness, both passive observational-equivalence results, classical quantum-capability controls, ML-KEM opacity/total-key-recovery controls, strong-versus-weak same-identity ML-KEM re-encapsulation controls, exact HKDF prefix and endpoint controls, distinct-versus-aliased HKDF domains, strong-versus-weak ML-KEM/X25519 algorithm-tag and X25519-role confusion controls, original compromise, private state-neutral receive, and post-rejection compromise result sets; scenario classification; rejection of timeout/inconclusive/missing/substituted/unexpected results; and the required positive/negative secrecy, correspondence, equivalence, and reachability splits. |
| HB-12 | `Makefile`, `proofs/fstar/Makefile`, and `proofs/ssprove/Makefile` | Deny-all F* item selectors, no-exclusion Lean extraction, backend composition, interface-aware F* dependency generation, strict checker flags, all twenty-seven named ProVerif scenario invocations, SSProve assumption and kernel checks, generated drift checks, and this inventory gate. The Lean target must import every file whose theorems are claimed; merely leaving a proof file in the tree does not check it. |
| HB-13 | `proofs/pro-verif/failed-receive.pv` | Two exact, non-replicated server-to-beacon state-neutral receive legs synchronized with a private-state sink and composed with replicated attacker-owned beacon registration processes. Both record roots are fresh and standalone rather than established by the registration process. |
| HB-14 | `proofs/pro-verif/failed-receive-queries.pvl` | Six required private-state secrecy queries and eleven state/origin correspondences for exact rejection-state reuse, later honest acceptance, target consumption, replay rejection, skipped-key delayed delivery, maximum-gap success, cached consumption, after-release success, and honest-send provenance. |
| HB-15 | `proofs/pro-verif/failed-receive-reachability-queries.pvl` | Twelve required negative reachability negations covering both neutral rejections, every short-leg success/replay/delayed event, maximum-gap success, capacity rejection, cached consumption, after-release success, attacker-owned registration commit, and malicious-canary recovery. |
| HB-16 | `proofs/pro-verif/failed-receive-compromise.pv` | The same exact state-neutral receive legs synchronized with disclosure of the legitimate receiver's unchanged empty-cache state immediately after rejection and composed with replicated attacker-owned beacon registration processes. Both record roots remain fresh and standalone. |
| HB-17 | `proofs/pro-verif/failed-receive-compromise-queries.pvl` | Six secrecy classifications and three correspondences: consumed-past and independently rooted capacity-leg values remain secret, unchanged-chain-derived skipped/target values and honest-send provenance are deliberate attacks, and later short-leg acceptance/delivery requires prior same-term compromise. |
| HB-18 | `proofs/pro-verif/failed-receive-compromise-reachability-queries.pvl` | Required negative reachability negations witnessing both the synchronized exact-state-equality compromise and a possible later authentic delivery. |
| HB-19 | `proofs/fstar/Beaconcrypt_core.Commitment.Lemmas.fst` | Strict proof that the extracted 229-byte commitment transcript contains the key, nonce, associated data, AEAD tag, sequence, and sender ID at the exact fixed ranges; both LE64 encodings and the complete six-field input are injective; and `ctx_distinct_openings_imply_hash_collision` returns a valid collision witness for any two semantically distinct accepted explanations of one fixed payload under arbitrary pure hash and AEAD-open functions. Checked proof, not an axiom file. |
| HB-20 | `proofs/pro-verif/aead-commitment-negative-control.pvl` | Deliberately non-key-committing AEAD theory in which one ciphertext/tag opens to two fresh plaintexts under distinct constructed keys, nonces, and associated-data contexts, plus otherwise identical with-CTX and without-CTX processes and six independent single-field mutation controls for key, nonce, associated data, retained AEAD tag, sequence, and sender ID. These constructor controls detect symbolic field omission; they do not prove production serialization or BLAKE2b behavior. |
| HB-21 | `proofs/pro-verif/aead-commitment-negative-control-queries.pvl` | The shared double-opening reachability query, required to have opposite classifications in the two top-level scenarios, plus six field-mutation queries that must remain unreachable in both scenarios. |
| HB-22 | `proofs/pro-verif/aead-commitment.pv` | Top-level CTX negative-control process; validation through the structured six-field preimage and collision-free ideal `blake2b512` constructor must make the double-opening event and all six field-mutation events unreachable. |
| HB-23 | `proofs/pro-verif/aead-no-commitment.pv` | Top-level no-CTX negative-control process; removing only the commitment checks must make the double-opening event reachable while the independent CTX constructor field controls remain unreachable. |
| HB-24 | `proofs/fstar/Beaconcrypt_core.Ratchet.fsti`, `proofs/fstar/Beaconcrypt_core.Ratchet.Control.fsti`, and `proofs/fstar/Beaconcrypt_core.Ratchet.Refined.fsti` | Stable generated-module boundaries with abstract representations. The root interface contains exactly the eight constants, types, and constructors directly imported by generated PQXDH; the Control and Refined interfaces contain only the accessors and operations required by their immediate generated consumers. The inventory gate rejects representation declarations and fixes the declaration counts and root eight-name allowlist. |
| HB-25 | `proofs/lean/BeaconcryptCore/Model/Ratchet.lean` | Handwritten ideal symmetric-ratchet driver and its mathematical properties. It is the specification side of refinement, not production code and not evidence that an extracted transition implements it without a separate relation theorem. |
| HB-26 | `proofs/lean/BeaconcryptCore/Refinement/RatchetControl.lean` and `RatchetControlRestore.lean` | Checked facts about the Aeneas-generated control plane, including counters, admission, cache behavior, and restoration. They constrain extracted functions but do not interpret KDF/AEAD effects or prove the concrete phase driver. |
| HB-27 | `proofs/lean/BeaconcryptCore/Refinement/RatchetRefinement.lean` | Checked refinement from the extracted receive control plane directly to the handwritten `Ratchet.recvStep` for positive wire targets at its 50-skipped-key bound, under the stated relation and authentication verdict. Wire sequence zero is outside that refinement theorem and is covered separately by state-neutral non-delivery. The target derivation is consumed separately, so exactly 50 skipped keys may remain cached. The file predates the concrete effect API and does not by itself cover direct chains/material, typed responses, send phases, PQXDH initialization, or the adapter loop. |
| HB-28 | `proofs/lean/BeaconcryptCore/Refinement/RatchetEffect.lean` | Imported and checked equations about generated current phase definitions: exact initial request/accessor/role offsets and core partition; ratchet response partition; send request, exhaustion, cancellation, resume, and finish result; receive request/cancellation, plan rejection, cached/future open selection and context; failed-open entry equality; and cached/future success publication returning the same plaintext. These are structural theorems, not an ideal-KDF or end-to-end refinement. |
| HB-29 | `proofs/lean/BeaconcryptCore/Refinement/RatchetEffectRefinement.lean` | Imported and checked phase-to-ideal results. `KernelRefines` precisely covers receive control, send sequence/bound, both chains, bidirectional live-slot/skipped-key correspondence, and empty slots above the live prefix; `ResponseRefines` assumes the exact `cr.kdfChain`/`cr.kdfMsg` response law. The file proves non-exhausted send begin/resume/ideal success and cancellation; receive KDF cancellation, open rejection/failure, and exact entry equality plus refinement preservation for supplied finite failed-trace witnesses; conditional open-reply consistency through the selected material and `cr.dec`; generated cached-open construction from `KernelRefines` plus an ideal lookup; cached exact material plus ideal success directly against cached `recvStep`; and the control-plane filtered-cache result for consumed cached removal. Concrete finish remains conditional on its generated publication equation, and full post-state refinement remains conditional on `CachedPublicationRefines` because the material-array swap/empty-suffix relation is unproved. Remaining work is to complete that cached material-array relation, prove future KDF staging/publication, compose initial roles and restoration, verify the adapter driver, and model ideal send exhaustion. |
| HB-30 | `proofs/pro-verif/{passive,passive-strong-secrecy,active-quantum,quantum-capability-control,quantum-mlkem-control}.pv` and their query/capability/witness libraries | Four-modality symbolic controls. The passive driver is one bounded honest-only session; fixed-canary secrecy is paired with an active-scheduler control that names every one of the five receive paths, and the parameterized biprocess separately requires observational equivalence for all five simultaneous plaintext choices in both classical and modeled-quantum modes. Passive quantum requires Ed25519/X25519 recovery canaries to be exposed, requires an isolated ML-KEM-derived canary to remain hidden under those classical recovery rules, and pairs that result with a deliberate total ML-KEM secret-key-recovery rule under which ordinary decapsulation exposes the canary. These ML-KEM controls test opacity versus total key recovery and do not test cross-key re-encapsulation. The active-quantum process is a bounded existential attack witness with private recovery operations, not an exhaustive active-QPT model; its exact recovery event carries the selected replacement ML-KEM public key, server ephemeral key, exact KEM ciphertext, initial frame, complete response, ordered root input, root, and recovered plaintext, and its four expected-false queries include failures of both honest initiation and authenticated-bundle agreement. All protocol-shaped paths reuse the extracted registration-ID, root-input, and associated-data operations. |
| HB-31 | `proofs/ssprove/BoundedRom.v`, `CtxEventReduction.v`, `CtxGame.v`, `expected-assumptions.txt`, `Makefile`, `_CoqProject`, and `README.md` | Generic fuel-bounded hidden finite-ROM runner and same-run reductions; retained abstract event-inclusion theorem; executable adaptive CTX binding game; two-query verifier; collision extractor; uniform specialization; `q + 2` trace bound; exact completed-run suffix; and non-vacuity witness. The build exact-diffs LA-08, rejects admissions and unsafe hax imports, keeps objects outside `proofs/`, and runs `coqchk`. Production-width types, numerical collision bounds, the F*/SSProve representation bridge, and a runtime theorem remain open. |
| HB-32 | `proofs/ssprove/CtxPrivacy.v` | Finite random-oracle table programming, a uniform key/table/digest source, key-preserving programming involution, true-real/programmed-real distribution and decision-probability equalities, adaptive same-run fundamental lemma, and `ctx_hidden_uniform_key_true_real_privacy_bound`, which bounds the absolute true-real/fresh-ideal decision gap by a secret-transcript query. The capstone uses direct finite-source reindexing and avoids the unaccepted marginal-probability interchange lemma. It is a classical leakage-preservation hop conditional on ideal AEAD confidentiality, not a numerical production-key bound, complete transformed-AEAD proof, ordinary-integrity theorem, or QROM result. |
| HB-33 | `proofs/ssprove/PqxdhRatchetGames.v` | One-session, one-record, fixed sequence-zero ideal PQXDH and symmetric-ratchet game with explicit statically selected forward/replacement network actions, production root-input order, distinct honest/substituted root entries, correlated Ed/X compromise, typed HB-39 root/initial/step uses, shared KDF prefix consistency, and causal attacker pad recomputation from the accepted fixed substituted input within the capability abstraction. The abstract record selects the second initial half and is not production's first response. Direct finite pushforward capstones prove advantage zero for forwarding and for replacement tampering under the fixed intended verification key, plus forward-only passive-classical and passive-quantum capability models, and prove advantage one for active-quantum replacement. A self-consistent attacker-owned identity and signed bundle is outside the classical replacement action. The executable package body is linked to the pure sample computation, but package-interpreter distribution equality is deliberately absent because the pinned high-level bridge imports an unaccepted dependency. A contextual equivalence to a byte-string-indexed HKDF oracle, attacker-facing KDF queries, a public-transcript API, record tampering/decryption, arbitrary schedules, multiple users, primitive reductions, CTX composition, QPT adversaries, and QROM access are outside the theorem. |
| HB-34 | `proofs/ssprove/PqxdhRatchetRom.v` | Attacker-facing bounded adaptive classical-ROM confidentiality hop for the three positive forwarding cases plus active-classical replacement. A finite tagged oracle shares root and symmetric-ratchet answers with the challenger; four DH atoms are fixed and guessable, only the honest ML-KEM atom and table are hidden, and the observer receives the challenge ciphertext. A table-dependent involution flips only the record-pad component while preserving its own query index. The fundamental lemma makes every coupled decision mismatch imply that the trace queried that exact index, and the capstone bounds advantage by its bad-query probability. A restricted scenario type plus a checked hidden-root capability lemma excludes active-quantum replacement; active-classical replacement is exactly zero, while the separate HB-33 active-quantum replacement remains advantage one. The one-bit symmetric domain can be exhausted with two queries, so no negligible or numerical production-width bound follows. There is no general transcript or decryption interface, adaptive network schedule, package-interpreter equivalence, QPT, or QROM result. |
| HB-35 | `proofs/ssprove/PqxdhHybridSecurity.v` | Bounded adaptive classical-ROM structural robustness hop for the ordered five-coordinate PQXDH root input. One selected DH or ML-KEM contribution is uniformly hidden while all other coordinates are arbitrary public inputs; advantage for a one-bit payload masked by the modeled root output is bounded by querying the corresponding five-coordinate input. The ideal lookup uses HB-39's proved-injective encoding of the exact production info strings, but fixed padding and HKDF-SHA-512 Extract/Expand remain collapsed into that lookup; this is not an extraction-linked real-or-random root-key theorem. Capability corollaries cover active-classical, passive-classical, and passive-quantum forwarding, while separate lemmas expose the passive-quantum classical coordinates and show that active-quantum replacement has no hidden hybrid component. The result supplies no production-width guessing bound, multi-session composition, QPT, or QROM theorem. |
| HB-36 | `proofs/ssprove/RatchetForwardSecrecy.v` | One-step erasure-conditioned symmetric-ratchet confidentiality hop. The hidden source contains the predecessor chain and complete finite table; the public view contains the compromised next chain, prior challenge ciphertext, and prior nonce. A measure-preserving table involution flips only the predecessor step's message-key slot while preserving its next-chain and nonce slots, and the capstone bounds advantage by the probability of querying the exact erased predecessor-chain input. The finite tag comes from HB-39 and covers exactly the distinct root and symmetric production labels, not initial-versus-step symmetric expansion. The theorem assumes erasure by omission and supplies no session, direction, sequence, multi-step, implementation-erasure, numerical production-width, QPT, or QROM result. |
| HB-37 | `proofs/ssprove/RecordIntegrity.v` | Finite one-record/one-candidate game assuming a single ideal random-function authenticator for the combined direction-specific AEAD and CTX acceptance decision; composition of those mechanisms is not proved. The public context represents direction, associated data, CTX sender identifier, nonce, and sequence without claiming that AEAD alone binds the last two fields. The game exactly partitions active modifications by prior-query status, makes fresh-input and fresh-guess events definitionally identical, and extracts a same-run unequal-input/equal-output table witness from same-payload cross-context or cross-sequence reuse. Identical-context replay, a standalone collision game, a numerical production authenticator bound, general verification-oracle composition, multiple records, primitive correctness, and implementation refinement are outside the theorem. |
| HB-38 | `proofs/ssprove/RecordIntegrityBound.v` | Exact finite fresh-guess bound for the one-bit uniform combined-authenticator table. A table-dependent involution flips the completed candidate's fresh, unqueried entry while preserving the honest payload and adaptive adversary execution; it toggles success and proves `Pr[fresh accepted candidate] <= 1/2` for the exact bounded verifier runner. This is a numerical theorem only for the one-bit ideal table, not a production-width tag bound, separate AEAD/CTX composition, multi-record verification theorem, QPT result, or QROM result. |
| HB-39 | `proofs/ssprove/ProtocolLabels.v` | Closed structural model of the exact 46-byte `PQXDH_INFO` and distinct 41-byte `SYM_RATCHET_INFO` strings, a proved-injective Boolean encoding used by the finite ROM tables, the 32/64/76-byte KDF uses, the intentional shared initial/step symmetric domain, and the exact 87-byte associated-data label suffix. The module neither invents a CTX label nor claims that output length separates equal HKDF inputs. Equality between the handwritten Rocq strings and extracted Rust arrays, prefix-consistent real/ideal HKDF semantics, primitive reduction, and collision accounting remain open. |
| HB-40 | `proofs/ssprove/StateSeparatedComposition.v` | Single-run structural keying/keyed composition slice with a typed private two-slot consuming CKEY provider. Pure capstones establish `Empty -> Full -> Taken` transfers with cleared tombstones, rejection of overwrite, retake, wrong-role, and wrong-session access, server `(send=L, receive=R)` and beacon `(send=R, receive=L)` orientation, authenticated use of `L` for the production sequence-one response, challenge-independent rejection of false provenance, exact HB-39 root/initial/step labels and 32/64/76-byte sizes, and pointwise public-view equality with the monolithic single-call calculation. The exported `RUN : unit -> option ciphertext` samples the complete eight-bit finite tape internally through one joint operation, returns `Some ciphertext` after the modeled honest open, returns `None` before invoking the core on false provenance, and never returns the tape or opened plaintext. A challenge-indexed game pair and direct uniform-source pushforward use the optional-ciphertext view, but neither is an assumption-reported package-distribution theorem. Closed scenario snapshots record replay consumption before construction, construction failure without server publication, published-response drop asymmetry, accepted `(server send=1, receive=0; beacon send=0, receive=1)` counters, and pending-beacon rejection that preserves prior server fields. The record remains a one-bit XOR harness without nonce, associated data, AEAD, CTX, identifiers, or serialization; the Boolean provenance and accepted root input are parameters; the public API has no active scheduling or reveal operations; arbitrary non-substituted roots collapse to the honest finite-ROM entry; and lifecycle snapshots are not package reachability. This consuming `PUT`/`TAKE` store is not the paper's repeatable `GET`/`HON` CKEY, the one-shot package is distinguishable from the stateless monolithic harness on a second call, and no package-interpreter equivalence, production-left-half confidentiality reduction, three-side/four-corner advantage bound, authenticity, intended-peer agreement, production-width bridge, or Rust refinement is proved. |
| HB-41 | `proofs/ssprove/StateSeparatedPackageSemantics.v` | Proof-carrying certificate for the exact finite one-call optional observation and complete final heap produced by HB-40's linked public `RUN` operation from the canonical empty heap; it does not claim an execution-trace relation. A restricted evaluator supplies one indexed value to the sole joint finite sampler and executes the projected raw code with SSProve's actual heap operations, while local bridge lemmas check that staged proof-erased projections are exactly the computational fields of the linked keying, keyed, CKEY, and driver packages. The authenticated certificate normalizes to HB-40's `Some ciphertext` observation and `both_consumed_heap`, while false provenance returns `None` before sampling or core invocation and retains `empty_heap`; reportable summaries show both private CKEY slots respectively `Taken` and `Empty`. The authenticated public marginal agrees pointwise with HB-40's direct observation, and the resulting uniform finite-source view probability and distinguishing advantage equal the existing direct view and advantage for every deterministic predicate. The executed bodies retain HB-39's distinct root label, shared initialization/step symmetric label, and exact 32/64/76-byte uses without inventing any additional domain tag. In pinned SSProve 0.2.4, assumption-reporting a capstone that retains generic package/raw sampler syntax traverses the rejected infinite-sum interchange dependency, so the accepted capstones expose summarized kernel-reduced certificate normal forms and direct finite probability rather than stock `Pr`, `Pr_op`, package-validity, interchange, or contextual-advantage results. HB-41 alone proves neither repeated-call nor unrestricted contextual equivalence: a second call observes HB-40's tombstones, and the active protocol API, production-width primitives, confidentiality/authenticity reductions, multi-session scheduling, representation bridge, and Rust refinement remain open; HB-42 supplies only the bounded two-handle stateful relation. |
| HB-42 | `proofs/ssprove/StateSeparatedIndexedSessions.v` | Bounded two-handle extension of HB-40/HB-41 with four private role/session CKEY slots and one outer cache for the complete finite ROM sample. The first fresh authenticated handle consumes one supplied sample, caches it, and reaches its two `Taken` tombstones; the fresh opposite handle reuses that same sample without consuming the residual stream and reaches a disjoint pair of tombstones; false authentication and same-handle reuse return `None` while preserving the exact heap and stream. Exact raw-code normalization lemmas connect those executions to a stateful reference, and the adaptive capstone permits a deterministic context to choose the second Boolean handle from the first public observation while proving equality of both public observations and residual sampling behavior. Sharing one complete sample preserves consistency for equal root and symmetric queries across handles within the model's two Boolean ROM buckets. The handle is ghost routing state for private slots and static per-handle parameters, not a production session identifier or a KDF input: no session, role, direction, sequence, or initialization-versus-step label is added, and the linked bodies retain HB-39's distinct root label, shared symmetric label, and 32/64/76-byte uses. This is not production registration-replay semantics, arbitrary multi-user or active-schedule composition, a stock unrestricted SSProve contextual theorem, a confidentiality/authenticity reduction, a production-width bridge, or Rust refinement. |

| HB-43 | `proofs/lean/BeaconcryptCore/Computational/VCVioFeasibility.lean`, `proofs/lean/lakefile.toml`, and `proofs/lean/lake-manifest.json` | Checked feasibility pilot over VCVio commit `cbd4144b51d92da00dd50f05e068b2348fa6e529`. It specializes upstream tight and looser collision-resistance-chain bounded lazy-ROM binding theorems to the Aeneas 229-byte transcript field and a 512-bit range, and proves exact one- and two-call behavior for a simplified linked private consuming-key store. `#print axioms` reports only the standard Lean classical/quotient axioms for the probability and extracted-constant theorems and none for the four state equalities. It does not embed beaconcrypt's actual CTX verifier/win event into the generic binding game, connect semantic production openings to distinct transcript bytes, reduce real BLAKE2b collision resistance, prove CTX privacy/integrity preservation, model the accepted two-slot result-and-final-heap certificate or arbitrary contexts, or establish end-to-end computational security. The upstream repository contains a wider evolving surface; this report is not a global admission, `unsafe`, or option audit. |
| HB-44 | `proofs/lean/BeaconcryptCore/Model/Pqxdh/*.lean`, `proofs/lean/PQXDH_IDEAL_MODEL.md`, and `proofs/lean/pqxdh_spec.md` | Handwritten ideal modified-PQXDH model, behavioral theorems, concrete non-vacuity instances, local CTX commitment hypotheses, and whole-run properties. The primitive interface assumes correctness and output lengths rather than computational security, and the model alone proves no production correspondence. |
| HB-45 | `proofs/lean/BeaconcryptCore/Refinement/Pqxdh{Bytes,Commitment,Core,Encoding,Protocol,Session}.lean` and `proofs/lean/PQXDH_REFINEMENT.md` | Checked abstraction/refinement from Aeneas-generated byte encodings and deterministic crypto-free registration transitions to the ideal PQXDH model, conditional on the generated inputs abstracting to the values computed through `Pqxdh.Crypto`. The commitment refinement equates the current generated 229-byte transcript builder with the ideal CTX preimage. Record sealing/opening belongs to the separately refined ratchet/adapter boundary. These files do not establish concrete primitive semantics, adapter authentication provenance, the adapter hash invocation and field provenance, Hax/Rust semantic preservation, compiler correspondence, or end-to-end security. |
| HB-46 | `proofs/lean/BeaconcryptCore/Computational/*.lean` and the collision-extraction theorem in `proofs/lean/BeaconcryptCore/Model/Pqxdh/Commit.lean` | Checked native ideal-model CTX reductions. `CtxMisattribution` runs the actual ideal `openRecord` parser and verifier on one arbitrary raw payload and two well-formed distinct explanations. The suite proves factor-one VCVio collision-resistance bounds for direct equal-seal commitment, raw-payload misattribution, retained-tag projection, and the classified full-forgery event; isolates context-alias inputs from honest seal inputs under the stated nonce condition; bounds alias acceptance in the shared lazy-ROM game by a hidden-key-prefix query plus `2^-512`; proves an exact canonical-cache isomorphism to a non-prefix complete-input cache plus a hidden-prefix key-free suffix cache; lifts canonical public and seal transitions and the complete adaptive run exactly into that split state; and carries record-opening results through reordered-record and foreign-session admission while proving exact-zero wrong-sender admission. The operational independent-tag handler sends every adversary ROM call only to the public cache and every honest outer tag only to the key-free suffix cache. One sticky-bad fundamental-lemma application proves that its uniform-key split transcript differs from the canonical split transcript by at most the public hidden-key-prefix event, with coefficient exactly one. Reachable-state invariants prove that unused nonces force fresh honest suffixes, that every honest commitment remains cached at its exact suffix, and that the complete lazy-suffix execution equals a handler drawing one explicit independent uniform 512-bit digest per successful seal; repeated nonces reject before sampling, so this hop has zero loss. The direct retained-base branch reduces with factor one to nonce-rejecting modified nonce-AEAD INT-CTXT. The key-probe reduction bounds the canonical secret-prefix event by modified key-probe IND$ advantage plus `qH / 2^256`. A conventional Boolean IND$ validation reduction uses one extra encryption at an unused nonce when `qE < 2^96`; real key hits validate, while a fresh random 128-bit tag costs at most `qH / 2^128`. The final actual typed one-key authenticity theorem therefore proves `Pr[full-fresh modified-CTX forgery] ≤ Adv_INT-CTXT + ofReal(Adv_IND$) + qH / 2^128 + 2^-512`, charging the secret-prefix event exactly once; the specialized key-probe capstone remains as a tighter alternative. Separately, the Boolean privacy module compares the canonical real sealing view with an explicit length-preserving ideal view. Its nonce-AEAD view reduction has exact real/direct and random/ideal experiment equalities, and its structural theorem charges only the view IND$ advantage plus the same prefix event. Substitution gives distinct view and probe IND$ advantages plus `qH / 2^256`, or distinct view and conventional validation IND$ advantages plus `qH / 2^128` when `qE < 2^96`; it has no INT-CTXT, collision, birthday, `2^-512`, or honest-commitment guessing term. Machine-checked accounting gives source total `qH + qE`, view/probe seal bounds `qE`, validation seal bound `qE + 1`, reduction-side uniform bound `64(qH + qE)`, supported primitive-interface bounds `64qH + 65qE` and `64qH + 65qE + 1`, 32 key bytes in real authenticity primitive games, and `96 + 64(qH + qE)` uniform-byte queries for the complete direct authenticity game. Privacy's fixed reduction-side counts exclude challenger-private length-matched bodies and 16-byte tags because no plaintext-byte budget is assumed. Guarded `#print axioms` reports only the documented standard Lean classical/quotient axioms and no `sorryAx` for the privacy exact-game equality and capstones. These results remain conditional on the stated model and primitive assumptions and do not establish raw-wire normalization, multi-key or indexed CAE composition, asymptotic PPT preservation, QROM security, end-to-end ProVerif interpretation, or the remaining adapter, Hax/Rust, and compiler bridges. |
| HB-47 | `proofs/pro-verif/hkdf-{prefix-conformance,endpoint-controls,domain-alias-control}*.{pv,pvl}` and `proofs/pro-verif/hkdf-domain-distinct-control.pv` | Focused symbolic HKDF controls require both shared-prefix equalities and two correct endpoint commitments, require every wrong-root-domain, wrong-symmetric-domain, and wrong-projection fixture to reach its attempted opening without committing, preserve a canary under the two distinct production-domain representatives, and disclose that same canary only when the control theory deliberately aliases both domains. These are symbolic conformance and security controls, not a concrete HKDF theorem, extraction linkage to the handwritten SSProve model, or a computational consequence of HB-50 synchronization. |
| HB-48 | `proofs/pro-verif/public-key-confusion-control.pvl`, `proofs/pro-verif/public-key-confusion-control-queries.pvl`, `proofs/pro-verif/public-key-confusion-strong.pv`, `proofs/pro-verif/public-key-confusion-weak-theory.pvl`, and `proofs/pro-verif/public-key-confusion-weak.pv` | Focused production-theory controls require legitimate ML-KEM, X25519-prekey, and X25519-one-time parsing plus both confusion attempts to be reachable while ML-KEM/X25519 algorithm confusion and X25519 prekey/one-time role confusion remain rejected. Independent deliberately weakened encoding theories alias only the relevant algorithm or role distinction and must make the corresponding acceptance witnesses reachable. These are symbolic disjointness and non-vacuity controls, not primitive or parser theorems; HB-50 separately checks the concrete marker and role facts. |
| HB-49 | `proofs/pro-verif/mlkem-reencapsulation-control.{pv,pvl}`, `proofs/pro-verif/mlkem-reencapsulation-control-queries.pvl`, and `proofs/pro-verif/mlkem-reencapsulation-{strong,weak}-theory.pvl` | Isolated same-identity multi-epoch differential outside the supported production lifecycle. One identity signs old and new bundles that reuse the classical material but select distinct ML-KEM public keys; only the old secret key is disclosed, the server selects the old bundle, the beacon awaits the new bundle, and both theories must reach the path, compromise, exact substituted-ciphertext attempt, and an ordinary one-shot completion. The strong theory retains PL-05 public-key binding, so weak re-encapsulation and root disclosure remain unreachable, the new-session canary stays secret, and the 14-field public-key/ciphertext commitment correspondence holds. The weak theory publicly re-encapsulates a learned secret under another key, making the weak-success and root-disclosure events reachable, exposing the canary, and breaking exact agreement. This does not model a current rotation API, derive public-key binding from IND-CCA, or change the production root input, associated data, or wire protocol. |
| HB-50 | `proofs/pro-verif/production-transcript-interface.pvl` and `tests/proverif_transcript_fidelity.rs` | Original 66-fact `@beaconcrypt-fidelity-v1` baseline, retained as a subset of the current interface consumed by every ProVerif scenario, plus a dependency-free checker against compiled core builders, reviewed adapter wiring, exact Phase-2 schema ordinals, the interface declarations, and the active-quantum witness. It covers the exact two domain literals, inequality, no salt, absence of output-length separation, shared initial/step domain and prefix, 32/64/76-byte outputs and splits, opposite server/beacon initial role orientations, `FF^32` and ordered 192-byte root input, all key markers and roles, server-first 153-byte associated data, unlabeled detached-tag AEAD inputs, unlabeled six-field LE64 229-byte CTX preimage, the five ordered Phase-2 response fields and their server/beacon mappings, absence of invented direction/sequence/session/phase labels, and all 18 ordered agreement fields including the selected ML-KEM key and ciphertext. Its original 40 in-memory fidelity mutations remain required: either label byte, aliased domains, separate initial/step domains, reversed AD identities, swapped X25519 roles, either omitted KEM agreement field, each independently omitted CTX field, every Phase-2 manifest fact, declaration/process/witness order permutations, reordered, omitted, or renamed Phase-2 schema fields, and a valid but incorrect source mapping for every Phase-2 server setter and beacon consumer. Every ProVerif target depends on this check and CI runs it independently. This is synchronization of enumerated deterministic facts, not model extraction, semantic refinement, primitive verification, compiler correspondence, or a computational protocol theorem. |
| HB-52 | `proofs/pro-verif/phase2-response-binding.pv`, `proofs/pro-verif/phase2-response-binding-control.pvl`, `proofs/pro-verif/phase2-assigned-id-strong-theory.pvl`, and `proofs/pro-verif/phase2-response-binding-queries.pvl` | Isolated production-theory Phase-2 response-binding control over one source-shaped beacon finish gate and four parallel fixtures. Its protocol gate reduces only when the authenticated eight-byte binding equals the binding derived from the outer `keyId`. It requires a genuine response commit, all three malformed attempts, internal gate reachability for the wrong outer identity and wrong inner sender, both the relabeled-ID branch's genuine-frame opening and observation of its original authenticated prefix, and each binding-correspondence antecedent to be reachable; rejects commits after changing only the outer server identity, only the outer assigned ID while retaining the authenticated original prefix, or only the inner sender of an otherwise genuine sealed frame; keeps the commit canary secret; and proves injective correspondences from the three accepted markers to the pinned outer identity, opened assigned-ID prefix, and pinned inner sender. The inventory locks the sole commit immediately after all three accepted markers. It reuses the canonical five-field response order, extraction-backed root input and associated data, shared KDF projections, and symbolic AEAD/CTX frame theory. It is a finite handwritten symbolic control, not Cap'n Proto parsing, adapter/source refinement, a primitive theorem, or arbitrary-session composition. |
| HB-53 | `proofs/pro-verif/phase2-response-binding-control.pvl`, `proofs/pro-verif/phase2-assigned-id-weak-theory.pvl`, `proofs/pro-verif/phase2-assigned-id-weak-queries.pvl`, and `proofs/pro-verif/phase2-assigned-id-weak.pv` | Paired deliberately weakened assigned-ID-binding control over HB-52's identical fixture and finish process. The sole substituted rule makes `phase2_assigned_id_binding_gate(authenticated, outer)` reduce for every pair rather than only an equal pair. Fresh distinct original and relabeled IDs ensure that the unchanged genuine frame authenticates the original eight-byte prefix while only the response's outer `keyId` is changed. The weak theory must reach both unchanged identity/sender gates, frame opening, original-prefix observation, relabeled commit, and public canary disclosure; it must falsify the assigned-prefix correspondence while preserving the outer-identity and inner-sender correspondences and their independent one-field rejection controls. Its Make target first checks HB-52, where the same relabeled commit remains unreachable and the canary secret. No response, root, associated-data, sender, sequence, KDF, AEAD, CTX, signature, DH, or KEM theory is weakened. This demonstrates necessity of the source equality gate, not a production attack, primitive weakness, parser theorem, or computational result. |
| HB-55 | `proofs/pro-verif/production-transcript-interface.pvl`, `proofs/pro-verif/environment.pvl`, and `tests/proverif_transcript_fidelity.rs` | Exact deterministic Phase-1 `InitKex` source-fidelity extension to HB-50. Eighteen new facts bind the Cap'n Proto fields `identityKey @0`, `preKey @1`, `oneTimeKey @2`, and `pqKey @3`; Ed25519 attached-signature format; Beacon signing and setter mappings; Server identity decoding, three signature consumers under that identity, `InitKex::from_encoded` order, and distinct core algorithm/role gates; and every ordinary, malicious, and active-quantum symbolic constructor/producer/consumer/core site. The production Server's getter/verification evaluation order is identity then PQ, prekey, one-time, while the symbolic process evaluates the three pure signature/tag gates prekey, one-time, PQ; both are locked as deterministic facts, but the exact claim is semantic field-to-verified-buffer mapping rather than security-relevant order equality. A separately counted 163-case mutation matrix changes all 18 facts; exhausts four-field same-typed schema permutations, omissions, renames, and pairwise ordinal drift; exhausts wrong Beacon signature/setter and Server consumer/type-compatible `from_encoded` mappings; applies pairwise transpositions to every symbolic site; and independently trips every new core, identity, signature-reduction, and evaluation-order validator family. The existing fidelity Make target and dedicated workflow job execute this extension before every ProVerif result. It neither changes a primitive theory nor proves Ed25519, X25519, ML-KEM-768, Cap'n Proto parsing/compiler semantics, full Rust-to-ProVerif refinement, or protocol computational security. |
| HB-57 | `../beaconcrypt/src/schema/cryptoframe.capnp`, `../beaconcrypt/src/ratchet.rs`, `src/commitment.rs`, `proofs/pro-verif/production-transcript-interface.pvl`, `proofs/pro-verif/crypto.pvl`, and `tests/proverif_transcript_fidelity.rs` | Exact deterministic `CryptoFrame` source-fidelity extension. Forty-two new facts bind schema ID `0xef858976d7f7863b`; ordinals `seq @0`, sender `keyId @1`, payload `cipherText @2`; local target metadata versus serialized sender ID; source builder order payload, sequence, sender separately from schema order; caller-side empty-plaintext rejection; detached ChaCha20-Poly1305 `C,T`; the existing checked unlabeled 229-byte CTX transcript and 64-byte `U`; exact `C || T || U` serialization; typed parse, empty-wire/sender/length gates before ratchet selection; one-use send/open material, sequence, context, and finish calls; exact tag/commitment/ciphertext slices; libsodium `memcmp` equality rejection before decrypting only `C || T`; and result metadata. The symbolic boundary is exactly `crypto_frame(C,T,U,sequence,sender_id)`, with the same `T` used by AEAD and commitment and an ideal exact-constructor opening rule; it adds no target ID, direction, session, phase, AEAD label, or CTX label to frame/seal arguments. A separately counted 223-case matrix changes all 42 facts and independently exercises schema/source/core/model mappings, all nonidentity same-typed three-field permutations, payload omissions/duplicates, sender/target confusion, boundary drift, all pairwise six-field CTX transpositions plus arity-preserving omission/duplication, and mutable LE64/229-byte core layout. These are exact deterministic fidelity mutations, including strict source-spelling drift that need not change security behavior. Recording a libsodium `memcmp` call does not prove timing properties; the ideal rule does not prove Cap'n Proto, parser, compiler, primitive, Rust-to-ProVerif, or computational semantics. |
| HB-58 | `../beaconcrypt/src/server.rs`, `../beaconcrypt/src/beacon.rs`, `../beaconcrypt/src/ratchet.rs`, `proofs/pro-verif/production-transcript-interface.pvl`, `proofs/pro-verif/environment.pvl`, and `tests/proverif_transcript_fidelity.rs` | Exact deterministic endpoint caller-wiring extension. Fifty-six new facts bind ordinary Server target metadata, local sender, server-first rebuilt associated data, peer lookup/ratchet/expected-sender selection, and success-only acceptance; ordinary Beacon pinned target/expected sender, assigned sender, stored associated data, and single ratchet; registration's assigned target metadata versus candidate Server inner sender; candidate associated data; post-initial-send peer-map provenance; post-initial-open Beacon ID/context provenance; and the security-relevant publication orders. The parsed Server sender remains an untrusted lookup selector before the already-idealized open succeeds. Scoped ProVerif facts freeze exact frame-call and event arrays only for the finite main-honest and malicious-registration fixtures, not auxiliary controls or a universal production schedule. A separately counted 242-case matrix changes all 56 facts; directly mutates every covered source caller/provenance/order family; changes each argument and family count of eleven symbolic frame calls; changes each argument, count, and arity of ten symbolic event calls; and reverses each covered symbolic associated-data constructor. This does not prove parser/compiler/runtime refinement, arbitrary schedules, ratchet-state correspondence, primitive correctness or security, nonce uniqueness, replay/rollback, global key-ID uniqueness, multi-user composition, constant-time execution, or erasure. |

The baseline ProVerif record model intentionally has one receive program point per sequence and uses a stronger ideal exact-opening AEAD rule. The separate HB-20--HB-23 differential control, not that ordinary rule, demonstrates the symbolic CTX benefit against a deliberate multi-opening, HB-48 checks the production key-encoding distinctions against explicit weakened theories, HB-49 isolates the PL-05/AR-13 dependency with an unsupported same-identity rotation counterfactual, HB-50 synchronizes the original enumerated production transcript facts without turning those controls into extraction or computational theorems, HB-52 tests the corresponding source-shaped Phase-2 acceptance gates, HB-53 demonstrates the necessity of exactly the assigned-ID equality gate, HB-55 extends deterministic synchronization across the exact Phase-1 registration wire boundary, HB-57 extends it across the exact `CryptoFrame` schema/seal/open boundary, and HB-58 binds the covered endpoint IDs, associated data, selected ratchets, registration provenance, and finite main-model call/event fixtures. HB-31 and HB-32 add finite classical-ROM binding and privacy hops, HB-33 and HB-34 add closed and attacker-facing four-modality confidentiality games, HB-35--HB-38 add standalone hybrid robustness, one-step erasure-conditioned confidentiality, record-integrity event/collision hops, and the exact one-bit fresh-guess bound, HB-39 gives their finite KDF tags an exact production-label interpretation, HB-40 adds the first one-shot state-separated CKEY and production sequence-one lifecycle slice, HB-41 checks that slice's exact authenticated and rejected one-call result-and-final-heap outcomes plus its public finite-view and advantage equality, and HB-42 proves bounded repeated-call equality with a stateful two-handle reference for an adaptively selected second handle. HB-42 does not compose the earlier computational advantages, model production registration replay, or establish arbitrary multi-user or unrestricted SSProve contextual equivalence, and all production-width, active-schedule, extraction-bridge, and quantum limitations remain explicit. The dedicated state-neutral receive model gives finite rejection, later success, replay, delayed delivery, and exact capacity witnesses rather than an arbitrary-schedule theorem. HB-01 and HB-02 provide stronger general proof evidence only for the predecessor F* concrete snapshot. For current production, HB-26/HB-27 prove the extracted control-to-ideal foundation for positive wire targets directly at the ideal 50-skipped-key bound, with sequence-zero neutrality established separately; HB-28 proves exact phase equations, and HB-29 checks the precise kernel relation, an assumed KDF response law, non-exhausted send begin/resume/ideal success plus cancellation, rollback for supplied finite failed-trace witnesses, conditional open-result consistency, generated cached-open construction, the control-plane half of cached consumption, and conditional cached success. HB-43 establishes that selected VCVio ROM and state-handler proof seams compile over production-shaped extracted types, HB-44 and HB-45 add the ideal PQXDH model and deterministic extracted-code refinement, and HB-46 supplies the ideal CTX computational reductions; these results are not yet a composed production CTX claim because deterministic adapter synchronization does not prove hash semantics, provenance preservation, or Rust-to-model semantic preservation. The remaining bridge covers the material-array half of cached swap-remove publication, future receive staging/publication, initial role composition, restoration, ideal send exhaustion, and the adapter driver. Concrete HKDF/AEAD/ML-KEM semantics, a computational public-key-binding reduction, the adapter effect interpreter, complete process/parser/runtime fidelity beyond the enumerated HB-50--HB-58 boundaries, equal-root authentication provenance, restoration provenance, projected-output noncollision, and the AR-03/05/06/13/18 one-owner and lifecycle conditions remain external. The replay process remains an explicit one-owner, non-rollback model rather than a multi-replica theorem. Replicated attacker-owned beacons disclose their freshly generated secrets before registration, and the malicious path accepts valid self-signed bundles. That path strengthens honest cross-recipient isolation but proves neither replay behavior nor availability for malicious identities. The private origin tables implement AR-17 proof classification, not production ACLs. Attacker-owned registration does not activate `CompromiseReceiveState`; that separate negative scenario explicitly discloses a legitimate receiver's unchanged post-rejection state beyond the base threat model.

## Maintaining the inventory

`reviewed-inventory.txt` uses `category SHA-256 path`, with paths relative to `beaconcrypt-core`. It fingerprints the production Rust/schema surface, core/extraction inputs, tool and CI controls, generated artifacts, every handwritten backend file, the result classifier, the complete-state protocol regression in `../beaconcrypt/tests/protocol.rs`, the persistence receive-effect regressions in `../beaconcrypt/tests/server.rs`, and this document/checker.
The checker independently derives the production Rust/schema, core Rust, generated, and handwritten backend file sets. It also compares every file of any extension under `proofs/` with the manifest (the manifest itself is the sole implicit self-referential entry) and rejects symlinks in monitored trees, so a new helper cannot be hidden by its name, extension, or ignore status.

There is deliberately no automatic refresh target. For an intentional change:

1. review the changed production/proof boundary and update the applicable
   OR/PL/AR/LA/GE/HB entries;
2. regenerate the F*, ProVerif, and Lean backends and run every claimed proof;
3. inspect every generated and handwritten diff;
4. update only the affected manifest digests; and
5. run `make check-inventory` and `make check-generated`.

Hash churn is intentionally conservative. A comment-only edit to a monitored
file still requires an explicit baseline update, because the gate cannot
reliably distinguish semantic from non-semantic proof-boundary changes.
