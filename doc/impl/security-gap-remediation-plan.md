# Security-gap remediation on vcvio

## Baseline and integration ownership

The starting commit is `10efd2b` on `vcvio`, identical to `origin/vcvio` after a fresh fetch on 2026-09-05. The main working tree was clean. Existing branches and worktrees are unrelated preserved work; none will be reset, removed, or force-pushed. Findings are hypotheses to check against source, proofs, fixtures, and maintained reference documentation.

The integration agent owns this plan, milestone records, `doc/formal-verification-analysis.md`, `doc/formal-verification.md`, the reviewed trust-boundary inventory, root build orchestration, and deployment changes. Workstream agents own disjoint implementation and proof files in new worktrees under `/home/user/worktrees`, each on a `codex/security-*` branch based on this baseline. Agents provide proof explanations and remaining obligations in their own records in `doc/impl/`; the integration agent incorporates those into maintained references.

## Milestones and acceptance criteria

| Milestone | Owner and worktree | Concrete work | Acceptance criteria and dependencies |
| --- | --- | --- | --- |
| M0: reviewed plan | Integration, main checkout | Verify branch state, inspect actual definitions, allocate work and validation resources. | Clean baseline and remote equality recorded; this plan committed and pushed before implementation integration. |
| M1: KDF and composition | KDF agent, `security-kdf`, branch `codex/security-kdf` | Exhibit the public-input distinguisher; retain valid inequalities without a negligibility interpretation; add a meaningful secret-input game/theorem with explicit adversarial access and an independently justified assumption boundary; audit downstream composition and losses. | Lean checks with no added axioms or placeholders; public-evaluation negative control; exact secret-input and evolving-key boundaries stated; downstream claims traced; integration agent reviews explanation and assumptions. |
| M2: adapter and raw receive | Adapter agent, `security-adapter`, branch `codex/security-adapter` | Strengthen production request execution and acceptance correspondence, including raw decoding, field provenance, CTX verification and recovered plaintext, rollback and publication; add machine-checked facts where feasible and focused production regressions. | Full raw-input acceptance path delimited; no adversary-claimed plaintext needed for the new acceptance endpoint; malformed and valid inputs, AD/context substitution, replay and state rollback exercised; remaining parser/primitive/compiler obligations distinguished from formal results; complete mutation suite on integration. |
| M3: registration fidelity | Registration agent, `security-registration`, branch `codex/security-registration` | Extend the main ProVerif registration schedule to accepted later-sequence completion, strengthen faithful agreement properties, preserve exact-transcript controls where sound, synchronize fixtures and production regression tests. | Main model checks include intended later-sequence acceptance and invalid-completion rejection; strongest supported correspondence explicitly proved and reachable; production behavior preserved; affected generated artifacts reviewed; complete mutation suite if substantive Rust changes land. |
| M4: deployment and integrated audit | Integration, main checkout | Review concrete dependency/API/entropy assumptions, constructor panic handling, foreign-buffer erasure, and persistence enforcement; maintain assumption/composition issue table and all shared analysis and inventory. | Focused engineering regressions, synchronized affected bindings, precise trust-store contract and unkeyed-digest limitation; all integrated repository checks pass; each residual limitation has an exact boundary and next step. |

Workstreams M1, M2 and M3 start concurrently after M0. M4 engineering and reference audit proceed independently during those workstreams. Milestones may be integrated in dependency-compatible completion order; shared documentation and inventory updates accompany each integration. If one result depends on another, its branch incorporates the already validated `vcvio` milestone through a normal merge. Completion means review and integrated validation, not merely an agent commit.

## Shared-resource and validation strategy

Only the integration agent launches full mutation testing and final `make -C beaconcrypt-core verify`. Nix shell/proof access is serialized by explicit coordination with the integration agent; agents request a bounded proof-validation slot and report its completion. No agent runs Nix garbage collection. Use separate Cargo target directories when concurrent development builds need isolation. Keep validation logs in `/tmp` and record summarized evidence here or in milestone records.

For each integration: inspect its complete diff and proof assumptions, merge the completed branch without destructive resets, incorporate the plain-English explanation in `doc/formal-verification-analysis.md`, run relevant focused checks, run `make -C beaconcrypt-core verify` and `make -C beaconcrypt-core check-inventory` for core/proof changes, review extraction and inventory diffs explicitly, and run complete mutation testing after substantial code additions. Resolve every missed or timed-out viable mutant before marking that milestone validated. Run `cargo fmt --all`, `cargo clippy --workspace --all-targets --all-features`, build/tests and the two role-specific feature checks on integrated code. Affected foreign interfaces also require the release Go build and race tests, and the Python extension build and Python tests; format Go and Python where changed.

Before each push, fetch `origin/vcvio`, inspect divergence and reconcile by merge when necessary, preserving concurrent work. Commit the milestone evidence, push `vcvio` without force, and verify the remote object ID with `git ls-remote`. Report each validated milestone and pushed commit. After the final Nix-backed command, confirm all repository agents and processes have released Nix, then run `nix-collect-garbage`.

## Security boundaries that must survive every milestone

No result may conflate tests or source tripwires with formal semantic correspondence. Do not assume the desired protocol conclusion under a new primitive-sounding name. Standard primitive games, extra cryptographic assumptions, symbolic idealizations, and unproved composition steps must be separately identified, including correlated evolving keys, session separation, compromise and reduction losses.

Forward secrecy does not provide recovery from current-chain compromise; skipped keys, snapshots, copied secrets and retained plaintext limit past-message confidentiality. Core normal-return certificates do not establish whole-application memory/runtime safety; count the current certificates before quoting a total. Constant-time primitive calls and comparison do not prove constant-time protocol execution, and no timing attack is claimed without evidence. Self-signed beacon authentication does not authorize tasking; recipient policy belongs to the application unless separately implemented and verified. An unkeyed snapshot digest is a content identifier, not integrity, rollback resistance or linearizability.

## Progress

- M0 inspection: `AGENTS.md` read; clean baseline confirmed; fresh remote comparison `0 0`; fixed public-input KDF game, typed CTX game, adapter phase execution and documented external correspondence boundary inspected; existing later-sequence controls and main symbolic schedule located.
- M1–M4: pending implementation and validation.
