# Independent refinement review

## Review scope

The representation workstream independently inspected protocol composition through the server first-record milestone `eeeab2c`, the coordinator’s checked `ProtocolHistory.lean`, and server transaction/publication through milestone `ad2b498`. This is a statement-level and proof-structure review, separate from the author and coordinator build gates. The ratchet workstream independently reviewed `RepresentationBridge`, `ByteTraceRefinement`, and `BoundaryExecution` in the opposite direction; those reviews reported no blocking issue.

## Beacon protocol findings

The new `completeBeacon_refines` theorem does not assume an ideal receive result or a desired registration refinement. `BeaconBoundary` identifies the typed inputs with successful primitive conversion/decapsulation results, shared DH values, identities, and wire identifiers/sequences. Root and initial/ongoing HKDF premises are local primitive representation equations.

The proof connects extracted candidate preparation to exact root transcript and association bytes, derives the root from that candidate, runs the actual initial phases, and invokes the actual receive driver on that initialized kernel. `recvStep_commutes` supplies the byte-model plaintext/error and represented poststate. The authenticated plaintext parser preserves the first eight bytes exactly, and extracted binding authentication and commit determine the assigned identifier and pinned binding.

Every included beacon outcome is represented: pinned-identity mismatch, zero DH, bad sender, zero sequence, record rejection, short plaintext, binding mismatch, and success. Record rejection includes malformed/authentication failure and skip/admission rejection at the receive API's absent-plaintext projection. Success retains the complete ratchet states, while failure corresponds to the ideal aborted registration. Decapsulation/conversion failure and the ideal `notInitSent` alternative are outside the typed supplied-input boundary and must not be claimed as core executions.

The first-record observation boundary was checked against Rust as well as Lean. Core `ReceiveOpen.finish` returns the complete supplied optional plaintext unchanged, and `initialize_receive_refines` retains exact equality of that entire plaintext with the byte-model receive result. Core `beacon_commit` returns only the established binding/identifier, and the unchanged ideal `beaconFinish` likewise returns registration identifier/state without an application suffix. Consequently, `completeBeacon` and `completeHistory` observe the registration commit result; they do not claim to verify delivery of a registration application payload. The surrounding `beaconcrypt/src/beacon.rs` splits the first eight plaintext bytes and returns the remaining suffix. That adapter extraction/return behavior is outside the core-only theorem and must not be presented as a verified output. The intermediate theorem preserves the full bytes needed for a separate suffix-delivery proof.

The handwritten driver includes sender/sequence guards and the authenticated plaintext length/prefix parsing needed to compose the public extracted phases. Its theorem is about this explicit synchronous API sequencing, not all arbitrary low-level phase calls or the surrounding Rust adapter. The actual library implementing that sequencing remains an external correspondence obligation.

The strengthened `ProtocolHistory.completeHistory_refines` closes the requested explicit registration-to-actual-callback-history composition. The driver passes the exact kernel returned by `completeBeacon` into `BoundaryExecution.runTrace`, with no reset or replacement. The proof constructs `ByteKernelRefines` from the registration witnesses and applies the finite actual-callback theorem under the primitive-only `Correct` law for each action. It preserves the exact ideal registration result and established identifier/binding/associated data, every subsequent observation, and the full related final state. The existential byte schedule varies only the actual sealing outcome; equality of complete `Invocation` lists retains every send AD/plaintext and receive target/AD/ciphertext field. Later failures may vary with reached material because the actual callback driver threads the reached kernel. Registration failure halts before any history operation. `completeHistory_observed` correctly applies the result to arbitrary actual evaluations by injectivity of the deterministic extracted result. No new premise assumes either registration or history refinement. Review identified and the coordinator corrected one mechanical issue: an unused callback-law parameter on the result relation was removed, leaving that legitimate contract in theorem premises only. No semantic blocker remains in this composition. The coordinator reports the strengthened module passed its 2988-target build.

## Server findings

The completed `eeeab2c` proofs `initialize_send_refines` and `initialize_serverRecord_refines` connect actual server initialization and first-send phases to the exact byte-model record and advanced send state, with the complementary empty receive state. The proof sequences extracted begin/resume/finish operations, identifies the actual request material and wire sequence from the established send relation, and then applies exact byte send commutation. It uses a specified successful record interpretation, rather than a premise asserting a successful desired refinement. The extracted wire sequence is converted to the model index; the initial send relation supplies sequence one and index zero. The `serverRecord` specialization retains the exact identifier prefix, associated data, server identifier, and complete record. Its only additional root premise is byte equality with the chosen model root. No blocking issue was found; the author reports the committed module build passed.

`failed_candidate_differs_from_atomic_emit` identifies a real strict-refinement obstruction: with an available nonexhausted identifier, unchanged ideal `serverEmit` advances the allocation counter, while a failed concrete candidate seal returns the old counter. `finishCandidate_failed` also discards the transient advanced ratchet. The `idealPublication` abort closure records failure at the already consumed replay-token prefix and preserves its entire state; success and rejection retain the full ideal response/error and state. Its preservation rule correctly requires a property at that prefix as well as after atomic emission.

The completed publication milestone `7b93499` closes the earlier candidate-to-prefix integration obligation. `prepare_candidate_refines` derives the previous/next counters, assigned identifier, both identities, server identifier, ephemeral key, KEM ciphertext, and associated data from actual extracted preparation. `prepareAndFinish_refines` covers every stable-binding allocation outcome, deriving that candidate relation before executing initialization, first sealing, and extracted commit or abort. Exact rejected allocation errors retain the external prefix; successful publication retains the complete response, exact peer fields, and a full byte relation for the advanced kernel. Failed sealing discards the transient ratchet and preserves the whole external entry, including the symbolic peer/replay-token context absent from the core. This relation prescribes an external bookkeeping contract and does not verify an adapter implementation. No blocking issue was found in the completed composed proof; the author reports its 2984-target build passed. The strict atomic counterexample remains explicit; the abort closure is not equality with the original atomic transition.

The record prefix was also traced to the extracted core-owned accessor. `pqxdh.ServerRegistrationCandidate.key_id_binding` is definitionally the extracted `pqxdh.registration_key_id_binding candidate.key_id`, and `PqxdhRefinement.registration_key_id_binding_abs` proves its full eight output bytes equal `Pqxdh.LE64 candidate.key_id.val`. Thus appending the unchanged application bytes yields the exact plaintext used by the publication record interpretation. The final `candidate_binding_prefix_exact` helper explicitly instantiates this extracted accessor correspondence and retains the entire prefix plus application, closing the requested handwritten-driver input correspondence.

The root-provenance milestone `1e4bd1a` was independently reviewed. `server_accept_root_input` derives the exact root transcript from an actual accepted extracted phase and the DH/KEM bytes supplied to that phase. `accepted_root_refines` applies the primitive `RootKdfLaw` to that transcript; it does not assume protocol-root equality. The surrounding DH/KEM implementations remain external primitive boundaries. The final `accepted_prepareAndFinishWith_refines` theorem consumes this provenance helper and computes `derive pending.root_key_input` from the same pending value in the actual accepted execution. `CandidateSealCorrect` constrains only successful callback returns to the complete expected record and actual sequence representation; failure is unrestricted. `finishCandidate_callback_covered` and `prepareAndFinishWith_covered` select only the actual outcome annotation and preserve equality of the entire actual driver evaluation. The capstone then applies the preparation/publication refinement and rewrites the root using `accepted_root_refines`. No root-equality or desired-refinement premise is assumed. The actual callback driver separately retains exact changed-binding rejection before initialization or sealing. The generalized `sealNext_optional_callback_covered` helper was also independently inspected and correctly preserves any specified complete successful callback output, including the actual sequence. No blocking issue was found in these final extensions. The author reports the final 2990-target build passed and direct axiom audits of callback coverage, prefix correspondence, and accepted publication contain only `propext`, `Classical.choice`, and `Quot.sound`.

## Property-transfer review

The byte and callback trace statements preserve complete public invocation fields, full records and plaintexts, failure observations, and related final byte states. Their transfer theorem is for universal finite predicates proved on the explicitly defined byte API wrapper. Properties of raw ideal atomic steps require a separate argument that they tolerate withheld failed sends and explicit API stutters. Neither the model-property premise nor the primitive-callback contracts assert implementation refinement. Infinite liveness, probabilistic security, hyperproperties, compiler/extraction faithfulness, external library correctness, and memory erasure are outside these finite behavioral statements.
