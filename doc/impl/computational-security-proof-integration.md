<!-- SPDX-License-Identifier: 0BSD -->

# Computational security proof integration evaluation

Status on 2026-09-02: the ProVerif suite names and checks the four required attacker scenarios and synchronizes its enumerated cryptographic transcript boundary with production through one CI-checked canonical interface, while the SSProve suite contains bounded hidden-ROM CTX binding/privacy hops, a closed one-session one-record ideal PQXDH-and-ratchet game, an attacker-facing bounded classical-ROM extension, a proof-carrying certificate for the exact one-call semantics of the state-separated response package, and a bounded two-handle repeated-call relation. The closed game proves zero advantage for active classical, passive classical, and a passive-quantum classical-query capability model, while active quantum has the expected advantage-one attack. The extension reduces each positive forwarding case and all active-classical actions to the exact hidden-pad-query probability but supplies no negligible production-width bound. The package certificate proves the authenticated and false-provenance normal forms from an empty heap and equates its public finite-source view and advantage with the existing direct calculation. The indexed extension proves equality with a stateful reference when a deterministic context chooses the second handle from the first public observation, using one globally cached complete finite sample and ghost handle routing that adds no KDF label. The synchronization check is neither extraction/refinement nor a computational theorem, and the overall suite is not production registration-replay semantics, unrestricted SSProve contextual composition, a complete computational proof of arbitrary beaconcrypt executions, or a QROM theorem.

## Decision and separation of concerns

The computational proof must reason about protocol security games, adversarial advantage, reductions, and composition assumptions. It assumes that the Rust operations supplied to those games implement their stated deterministic contracts. Lean and F* own implementation correctness, extraction refinement, fixed-size layouts, and state-machine correctness; a temporary Lean or F* failure caused by implementation/extraction reorganization is therefore not a computational-model counterexample and is not repaired as part of this work.

This separation does not permit an undocumented implementation assumption. Every deterministic fact consumed by a computational theorem must either be a named contract with a representation bridge to a named Lean or F* result or be identified explicitly as a handwritten model shape whose bridge remains open. The computational proof remains conditional when that bridge has not been reviewed against the current extraction snapshot.

The integration has three deliberately different evidence layers:

- ProVerif checks symbolic reachability, secrecy, and correspondence under ideal constructor and destructor equations.
- SSProve checks probabilistic event and game reasoning under explicit primitive and deterministic contracts.
- Lean and F* discharge deterministic contracts about the extracted implementation without being asked to prove cryptographic primitive security.

No result in one layer should be relabeled as a result from another layer.

## Four-scenario ProVerif matrix

The four named scenarios reuse the same hax-derived PQXDH types and builders and distinguish network control from classical-key recovery. The active and passive labels describe the network adversary; the classical and quantum labels describe the symbolic capabilities made available to that adversary.

| Scenario | Network power | Cryptographic capability | Checked result | Exact interpretation |
| --- | --- | --- | --- | --- |
| Active classical | Full Dolev-Yao interception, injection, deletion, replay, reordering, and attacker-owned self-signed registrations. | Ideal Ed25519, X25519, ML-KEM, HKDF, ratchet, AEAD, and CTX equations with no secret-key recovery. | All five secrecy queries and all seven injective correspondence queries are true, and the eight-query active reachability control finds its required witnesses. | The bounded per-session trace preserves the selected task and record canaries plus the listed origin, authenticated-bundle, replay-consumption, response-abort, complete-establishment, and record-origin correspondences in the symbolic model. |
| Passive classical | Transcript observation without network modification or injection over one bounded honest-only session. | The same ideal classical primitives as the active-classical model. | All five secrecy queries are true, and a shared active-scheduler control proves registration commit and record delivery reachable in the same process. | The selected transcript does not reveal the initial, cached, advanced, future, or beacon-to-server record canary. This is an explicit threat-model check and is logically weaker than the active-classical result. |
| Passive quantum | Transcript observation without network modification or injection over one bounded honest-only session. | Public symbolic recovery of secrets behind Ed25519 and X25519 public keys, while ML-KEM remains opaque. | All five secrecy queries are true, the shared active-scheduler control proves progress, and two expected-failure controls prove that both classical recovery rules expose their test secrets. | The selected hybrid transcript continues to hide the five canaries after the modeled classical-key recovery capability is enabled while the ML-KEM shared secret remains unavailable. This checks dependency structure only; it is not a computational quantum proof. |
| Active quantum | Full network control exercised by one bounded scripted man-in-the-middle. | Scripted recovery of Ed25519 and X25519 secrets, adversary-chosen replacement classical and ML-KEM keys, and no ML-KEM break. | Initial secrecy, the negated exact recovery-event reachability statement, honest-origin agreement, and authenticated-bundle agreement are all false as required. | The model contains a concrete trace in which the server accepts a forged registration under the honest beacon identity and the attacker derives and opens the initial tasking while recording the exact selected replacement key, response ciphertext, and derived transcript. Active post-quantum security is therefore refuted rather than claimed. |

The maintained entry points are [`baseline.pv`](../../beaconcrypt-core/proofs/pro-verif/baseline.pv) for active classical, [`passive.pv`](../../beaconcrypt-core/proofs/pro-verif/passive.pv) for both passive scenarios, and [`active-quantum.pv`](../../beaconcrypt-core/proofs/pro-verif/active-quantum.pv) with [`active-quantum-witness.pvl`](../../beaconcrypt-core/proofs/pro-verif/active-quantum-witness.pvl) for the bounded break. [`check-results.awk`](../../beaconcrypt-core/proofs/pro-verif/check-results.awk) fixes each expected classification, including the reachability and capability controls.

The active-classical query set consists of `attacker(INITIAL_SECRET)`, `attacker(CACHED_SECRET)`, `attacker(ADVANCE_SECRET)`, `attacker(FUTURE_SECRET)`, `attacker(BEACON_RECORD_SECRET)`, and the following seven injective correspondences:

- `ServerAccepted` implies the matching `BeaconInitiated` event.
- `ServerBundleAccepted` implies the matching `BeaconBundleInitiated` event with the same server identity, beacon identity, authenticated `InitKex`, registration ID, prekey, one-time X25519 key, selected ML-KEM public key, and registration origin.
- `ServerAccepted` implies the matching single-use `RegistrationConsumed` event.
- `RegistrationConsumed` implies the matching `BeaconInitiated` event.
- `ServerResponseAborted` implies the matching prior `RegistrationConsumed` event.
- `BeaconCommitted` implies the matching prior `ServerCommitted` event on one complete 18-field establishment transcript: server identity, beacon identity, authenticated `InitKex`, registration ID, prekey, one-time X25519 key, selected ML-KEM public key, server ephemeral key, exact KEM ciphertext, initial frame, complete response, ordered root input, derived root, exact associated data, assigned beacon key ID, pinned server key ID, session identifier, and registration origin.
- `MessageReceived` implies the matching `MessageSent` event with the same session, direction, sequence, sender, receiver, and plaintext.

The converse of beacon commitment is intentionally absent. A server can consume a registration and commit its response before that response is dropped, so `ServerCommitted` does not imply `BeaconCommitted`. Server-side confirmation of beacon receipt requires a later authenticated beacon-to-server record and is not supplied by the registration response alone.
The complete constructor prevents agreement from being discharged merely because two endpoints derive an equal ML-KEM shared secret or root; the exact public key selected from the authenticated bundle and the exact ciphertext returned to the beacon must also match.
This is equality in the handwritten symbolic term algebra, not a byte-level serialization or parser theorem, extraction linkage, a primitive computational reduction, or an end-to-end computational handshake theorem.
The selected ML-KEM public key and ciphertext are not added to the production root KDF input or associated data, and no production wire field changes here.

The baseline process has replicated roles and therefore admits multiple sessions, but each honest session follows a fixed record prefix: the registration task, an out-of-order downstream record that creates one cached key, delayed consumption of that cached record, the next future downstream record, and one beacon-to-server record. The result does not quantify over arbitrary record schedules, arbitrary cache contents, replication or rollback of persistent state, parser behavior, denial of service, or implementation side channels. Dedicated existing failed-receive and compromise scenarios provide additional finite traces but do not turn this bounded prefix into an arbitrary-schedule theorem.

Passive scenarios ask only confidentiality queries. Agreement and authenticity queries are omitted because an observer that cannot inject messages cannot exercise the attacks those properties are meant to exclude; passing such correspondences in a passive model would add little evidence and could hide vacuity.

The passive scenarios use `passive.pv`, which removes replicated malicious endpoints and runs one bounded honest beacon, one server, and the private state sink. Their parameterized roles also support `passive-strong-secrecy.pv`, a biprocess that simultaneously chooses alternatives for the initial, cached, advancing, future, and beacon-to-server plaintexts; both classical and modeled-quantum observational equivalence must hold. ProVerif does not classify positive reachability in passive-attacker mode, so both passive targets depend on an active-scheduler run of the same honest-only process that witnesses `BeaconCommitted` and each exact canary-bearing receive path. The passive-quantum target additionally publishes Ed25519 and X25519 public keys and requires both corresponding private canaries to become attacker-known, while an isolated ML-KEM-derived record canary must remain hidden. A paired deliberately weakened theory recovers the entire ML-KEM secret key from its public key and then uses ordinary decapsulation to expose that canary. These controls distinguish symbolic ML-KEM opacity from total key recovery; they do not exercise cross-key re-encapsulation and are not a primitive security proof.

The active-classical positive queries use the separately named reachability scenario as their non-vacuity control. Its expected reachable events and malicious-recipient canary exposure are checked independently rather than in the same ProVerif invocation.

The shared ProVerif KDF boundary now has one ideal no-salt HKDF-SHA-512 stream indexed by exactly the distinct PQXDH and symmetric-ratchet domains, and the extracted root-input replacement supplies the explicit `FF^32` field before DH1, DH2, DH3, DH4, and the ML-KEM secret. Initialization and record steps select the same symmetric stream projections, making initial-left equal step-key and initial-right equal step-next-chain for equal inputs without inventing a phase or output-length domain. Focused controls witness those equalities and correct endpoint execution, block wrong-domain and wrong-projection commitments after reachable attempts, and disclose a canary only in a deliberately domain-aliased theory. `ProtocolLabels.v` and the prefix-consistent SSProve ratchet model are independent design references rather than extraction linkage; the canonical ProVerif interface now checks the production byte strings and uses, while concrete HKDF security, production-width reductions, and the computational representation bridge remain separate obligations.

The authenticated-context boundary now represents associated data as tagged server identity, tagged beacon identity, and those same two KDF-domain values in the exact production order. Ed25519, ML-KEM-768, X25519 prekey, and X25519 one-time-key encodings remain symbolically disjoint, with strong/weak controls making algorithm-tag and role confusion observable. The base AEAD has no invented label and receives no sequence or sender ID in its associated data; the ideal CTX hash instead consumes the structured six-field production preimage with separate LE64 sequence and sender-ID encodings and no invented CTX prefix. This remains symbolic structure under injective serialization and collision-free-hash assumptions, while the canonical fidelity gate now synchronizes its marker bytes, byte lengths, offsets, and field order with compiled core behavior and adapter wiring.

## CI-enforced ProVerif transcript fidelity boundary

[`production-transcript-interface.pvl`](../../beaconcrypt-core/proofs/pro-verif/production-transcript-interface.pvl) supplies every scenario with one 465-fact machine-readable contract, and [`proverif_transcript_fidelity.rs`](../../beaconcrypt-core/tests/proverif_transcript_fidelity.rs) checks that contract against the compiled dependency-free core, mutable snapshots of the covered adapter, commitment, schema, and concrete-ratchet sources, exact Phase-1, Phase-2, and `CryptoFrame` schema ordinals and mappings, covered endpoint caller sites, the two normal-return ratchet message helpers, named checked Lean interpretation anchors, the finite receive-state fixtures and query files, the registration lifecycle and replay-key projection, the source-shaped initial-ratchet and later-sequence registration handoffs, and the ProVerif declaration, scoped process fixtures, active-quantum witness, and ideal frame-constructor sites.
The checked facts cover the exact four-field Phase-1 `InitKex`, attached Ed25519 signature shape and message-returning symbolic verification, the exact two labels, no salt, absence of output-length separation, 32/64/76-byte HKDF uses and splits, opposite initial role orientations, the shared initialization/step prefix, padded ordered root input, concrete key markers and roles, server-first 153-byte associated data, unlabeled detached-tag AEAD boundary, unlabeled six-field 229-byte CTX preimage with LE64 integers, the five production Phase-2 response fields, the exact `CryptoFrame` schema/seal/open boundary, absence of invented frame/seal direction, session, phase, or sequence-domain labels while retaining the wire sequence field, and both KEM fields in the exact 18-field establishment agreement constructor.
For Phase 1, the Beacon mapping is identity, signed tagged prekey, signed separately tagged one-time key, and signed tagged ML-KEM-768 key; the Server mapping verifies all three attached buffers under the decoded identity and passes identity, prekey, one-time, PQ to `InitKex::from_encoded`, whose core validation checks all four distinct tags and roles. Rust evaluates the signature getters PQ, prekey, one-time while ProVerif evaluates its pure gates prekey, one-time, PQ; the synchronization gate records both without treating evaluation order as security relevant.
For `CryptoFrame`, the wire carries sequence, sender ID, and `C || T || U`; local `Encrypted.key_id` is target metadata only. The checker separately records schema ordinal order, builder call order, and parser/gate evaluation order, then binds exact sender and length gates, selected one-use ratchet material/sequence/context, retained-tag commitment slices, commitment comparison before AEAD decryption, and the ideal symbolic `crypto_frame(C,T,U,sequence,sender_id)` constructor rule. The recorded libsodium `memcmp` call is not a timing proof, and the ideal exact-constructor reduction is not a Cap'n Proto or Rust parser-refinement theorem.
The endpoint extension binds production caller semantics on both sides of ordinary traffic and the initial registration frame. Server sends use peer target metadata and ratchet, local sender ID, and server-first rebuilt associated data; Server receives treat the parsed sender only as an untrusted peer lookup selector until open succeeds under that selected context. Beacon traffic uses its pinned Server ID, assigned local sender ID, stored establishment data, and single ratchet. Registration keeps assigned target metadata separate from the candidate Server's serialized sender and publishes the post-send/post-open state only after the checked authentication gates. Exact ProVerif frame-call and event arrays are scoped finite-fixture synchronization, not a claim that production traffic has the fixture's material, sequence, payload, or event schedule.
The initial-ratchet extension contributes 71 facts. It starts at the caller-supplied `RegistrationOutput` boundary rather than claiming that the public value remembers its producer, follows the exact 32-byte `KexDerivedSecret` alias and `SecretArr::as_array` field accessor into the role-specific core request, keeps the affine pending value separate from the non-provenance-carrying 64-byte response, checks one adapter-local HKDF construction and resume, and locks the complementary `0..32`/`32..64` kernel initialization with zero counters. It then checks existing Lean structural declarations and the ideal `rootChains`/post-initial-record `chain_agreement` names as text anchors only, plus the exact PQXDH-root binding in each of the three scoped ProVerif roles and exactly five chain uses with their material bindings. Complementarity assumes equal local roots and equal results from the same faithful deterministic response function; it does not prove the distinct root computations agree, identify two separately executed response objects, or equate Rust arrays with ProVerif terms.
The later-registration extension contributes 68 facts for one exact finite D9 witness. Production `Beacon::finish_registration` uses the ordinary receive helper and commits the returned staged ratchet after the assigned-ID binding check without reading the decrypted sequence or imposing a sequence-1 gate. The core-side boundary fixes the bound-50 future plan, skipped-key staging and validation, separate target material, successful publication, and the existing general-receive Lean anchor names. The finite ideal fixture has one genuine Server emit the original sequence-1 response before genuine sequence-2 and sequence-3 frames, holds every outer response field fixed while substituting only the application frame, and fans that one candidate to a faithful branch and a counterfactual first-only branch. Faithful completion returns the sequence-3 application remainder and exposes the exact ratchet term: Beacon send counter 0 and initial chain, receive counter 3 and chain 4, cache sequence 2/material 2 then sequence 1/material 1, with material 3 used for opening and absent from the cache. The first-only gate is deliberately placed after the same finite successful open, not at the source's evaluation point, and cannot commit or expose its canary. Exact correspondences require the selected substitution, same-session/root genuine Server sequence-3 send, and prior original response. These constructor equations do not prove arbitrary schedules, parser/compiler/serialization correspondence, semantic refinement, primitive correctness/totality/security, liveness, a production first-only gate, the complete established Beacon/PQXDH state, persistence, multi-user behavior, crash behavior, or erasure.
The 2,861-case in-memory mutation suite retains the previous 40 transcript/Phase-2 cases and adds separately counted 163-case Phase-1, 223-case `CryptoFrame`, 242-case endpoint, 154-case ratchet-driver, 1,230-case finite receive-state, 177-case registration-lifecycle, 223-case initial-ratchet, and 409-case later-registration matrices. The endpoint matrix changes all 56 endpoint facts, source caller ID/associated-data/ratchet and registration-publication mappings, every locked field and count of eleven scoped frame calls, every locked field/count/arity of ten scoped event calls, and all three scoped server-first ProVerif associated-data constructors. The driver matrix changes all 38 driver facts and independently locks the affine slot, typed KDF pending/request/response flow, normal-return send/receive branch and publication order, core publication placement, named structural and conditional Lean anchors, and atomic ProVerif abstraction. The receive-state matrix changes all 46 fixture facts and checks 48 core/constructor, 177 short-fixture, 720 capacity-fixture, and 239 exact-query controls, including both equality-gated destructor failures, the 50-entry cache boundary, target non-retention, cached release, and every field of the eleven correspondences and ten state-event reachability requests. The registration-lifecycle matrix changes all 60 lifecycle facts and directly locks the exact identity-plus-one-time replay ID, Beacon successful-serialization transition and post-Init graph, Server derive/consume/output order, finite honest replay-owner topology, malicious fresh overapproximation, and unsupported HB-49 field-reuse fixture. The initial-ratchet matrix changes all 71 facts and independently locks the shared-root alias/accessor, by-value cross-method handoff, both role starts, pending/response separation, 64/76-byte adapter distinction, split/orientation, zero-counter kernel, Lean anchors, and each scoped ProVerif root derivation, chain, and material use. The later-registration matrix changes all 68 facts and independently locks the no-first-gate source negatives, future-plan counters, staged-slot validation and publication, target separation, both role-shaped DH/KEM/root constructions, exact response substitution and one-candidate fanout, faithful/counterfactual gates, expanded chain/cache certificates, all 18 query formulas and result polarities, and scenario wiring. This is deterministic source-text synchronization, including some strict drift guards rather than executable security attacks; it does not prove semantic Rust-to-Lean or Rust-to-ProVerif refinement, HKDF-SHA-512 correctness or security, root agreement, key uniqueness, persistent multi-owner replay, arbitrary schedules, or any new primitive-security claim.
These are deterministic fidelity checks that fail even if a modified security model could still prove secrecy; the weakened HKDF, public-key-encoding, CTX, and KEM theories remain distinct security controls whose success condition is an explicit attack or failed correspondence.
The gate does not generate the protocol model, prove a semantic refinement from Rust to ProVerif, connect the handwritten SSProve games to extraction, or supply any primitive advantage bound, asymptotic result, computational composition, compiler theorem, or end-to-end implementation claim.

## Phase-2 response binding control

The isolated [`phase2-response-binding`](../../beaconcrypt-core/proofs/pro-verif/phase2-response-binding.pv) scenario reuses the canonical five-field response, extraction-backed root and associated-data builders, production KDF projections, and exact symbolic frame construction. A genuine response reaches commit; independently reachable variants with only a wrong outer server identity, only a relabeled outer assigned ID, or only a wrong sender ID in an otherwise genuine sealed inner frame do not. The wrong-identity and wrong-sender variants additionally reach their internal verifier comparison gates.

The assigned-ID variant must first open the unchanged genuine frame and observe its original authenticated prefix. Separate reachability queries exercise all three correspondence antecedents, while three injective correspondences link the accepted markers to the pinned outer identity, opened assigned-ID prefix, and pinned inner sender; the inventory fixes the sole commit immediately after those markers. This closes a symbolic source-shape gap between the canonical Phase-2 field mapping and the existing protocol correspondences, but it does not prove Cap'n Proto parsing, Rust-to-ProVerif refinement, concrete primitive security, arbitrary schedules, or computational composition; the deliberately weakened assigned-ID theory is kept in the separate paired control below.

The paired [`phase2-assigned-id-weak`](../../beaconcrypt-core/proofs/pro-verif/phase2-assigned-id-weak.pv) negative control now supplies that differential. Strong and weak scenarios share the exact fixture and finish process and differ only in the protocol-gate reduction: production reduces for equal authenticated/outer ID bindings, while the deliberate variant reduces for every pair. With fresh distinct original and relabeled IDs, the strong side rejects the relabeled commit and keeps its canary secret; the weak side reaches the unchanged identity, root/AD, sender, frame-opening, and original-prefix points, commits, exposes the canary, and falsifies only the assigned-prefix correspondence. The target runs the production control first, so the result identifies the necessity of the source equality check rather than a production attack or weakness in HKDF-SHA-512, ChaCha20-Poly1305, BLAKE2b-512, ML-KEM-768, X25519, or Ed25519.

## Active-quantum attack witness

The bounded attack intercepts the honest beacon's signed initialization, obtains the signing secret through the modeled Ed25519 recovery capability, and signs a replacement bundle containing attacker-chosen X25519 prekey, X25519 one-time key, and ML-KEM public key. The server accepts the replacement under the honest beacon identity and encapsulates to the attacker-controlled ML-KEM key.

After receiving the server response, the attack obtains the classical server secret through the modeled X25519 recovery capability, computes the four modeled DH contributions, decapsulates the ML-KEM ciphertext with its chosen ML-KEM secret, rebuilds the hax-derived ordered root input and associated data, derives the server-to-beacon chain material, and opens the initial frame. The same run emits `QuantumInitialSecretRecovered` with the selected replacement ML-KEM public key, server ephemeral key, exact KEM ciphertext, initial frame, complete response, ordered root input, root, and recovered plaintext; makes `INITIAL_SECRET` attacker-known; and gives both a `ServerAccepted` event without a matching honest `BeaconInitiated` event and a `ServerBundleAccepted` event without a matching honest `BeaconBundleInitiated` event.

The attack does not need to break ML-KEM because an active attacker that can forge the classical authentication substitutes its own ML-KEM public key before the server encapsulates. This is the central distinction between passive harvest-now-decrypt-later resistance and active post-quantum authentication.

The active witness uses private recovery functions inside one scripted process rather than giving unrestricted recovery destructors to ProVerif's active search. That restriction keeps resolution finite and is sufficient to exhibit a break, but it is not a complete symbolic characterization of every active quantum strategy. The public recovery destructors used by the passive-quantum scenario and the separate private recovery functions used by the scripted active witness are threat-model controls, not implementations or computational descriptions of Shor's algorithm.

## Computational meaning of the scenario matrix

ProVerif operates in a symbolic term algebra. Its positive results do not supply concrete advantage bounds, multi-user reduction losses, running-time bounds, quantum query semantics, random-oracle semantics, or proofs about the named primitive implementations. The four-scenario matrix is valuable because it fixes the intended security claims, finds symbolic dependency errors, checks correspondence structure, and provides the required active-quantum counterexample before the same claims are encoded as computational games.

In particular, the shared-stream equations faithfully remove the model's former extra KDF separation. HB-64 now locks the exact source-shaped initial-root request, 64-byte response, complementary split, and finite ProVerif uses, but this deterministic seam does not prove HKDF-SHA-512 pseudorandomness, Extract/Expand correctness, endpoint root agreement, semantic adapter fidelity, or compiler preservation. A computational treatment still needs the ordered padded combiner reduction, prefix-consistent 64/76-byte semantics under the shared symmetric label, security under the exact two production labels, and semantic composition of the checked source seam with the ideal models.

The active-classical symbolic result is the specification target for a classical computational protocol proof. The passive-classical result should follow as a restricted-adversary corollary rather than receive an unrelated reduction. The checked SSProve passive-quantum capability game exposes the classical secret-recovery consequences while keeping honest ML-KEM decapsulation opaque, but a genuine passive-quantum result still requires a QPT or carefully justified post-quantum lifting theorem over classical transcripts. Standard SSProve packages are classical probabilistic programs and do not model quantum state, superposition oracle queries, or QROM access.

The active-quantum result is a negative theorem and should remain an executable expected-failure test. Replacing Ed25519 or X25519 assumptions with quantum-secure names cannot make the current wire protocol actively post-quantum secure because the concrete attack substitutes an attacker-owned KEM key after breaking classical authentication.

## Bounded SSProve PQXDH and ratchet games

[`ProtocolLabels.v`](../../beaconcrypt-core/proofs/ssprove/ProtocolLabels.v) records the exact 46-byte `BeaconcryptPqxdh_CURVE25519_SHA-512_ML-KEM-768` and 41-byte `SymRatchet_HKDF_SHA-512_CHACHA20_POLY1305` strings, proves they are distinct, and gives the finite ROM tag a proved-injective interpretation in those production bytes. It records 32-byte root output, 64-byte initialization output, and 76-byte ratchet-step output and proves that the latter two calls share the same symmetric KDF domain. The closed game now supplies these typed uses to its actual root, initialization, and step calls, while the ROM query constructors use the common tag. This prevents an invented initialization-versus-step domain inside the handwritten games, but a contextual equivalence to a byte-string-indexed oracle and equality between the Rocq literals and extracted Rust arrays remain bridge obligations.

[`PqxdhRatchetGames.v`](../../beaconcrypt-core/proofs/ssprove/PqxdhRatchetGames.v) defines network power and computation power independently and instantiates all four combinations. One Ed25519 seed jointly controls signing and the converted X25519 identity, so the quantum capability compromises those correlated uses together rather than sampling independent secrets.

The modeled PQXDH root input contains exactly four ordered DH contributions followed by the ML-KEM contribution, corresponding to the production `build_root_key_input` contract after its fixed padding. Authentication metadata is deliberately absent from the root hash input. The symmetric KDF is one shared finite table for both initial expansion and later ratchet steps, and its abstract output has two common prefix components plus the later suffix so equal inputs enforce the production 64/76-byte prefix relation represented by `split_initial_ratchet_kdf_output` and `split_ratchet_kdf_output`.

The closed confidentiality experiment samples a jointly uniform eight-bit finite random-oracle tape with distinct honest and substituted root entries, establishes one session, advances one symmetric-ratchet record at fixed sequence zero, and masks a Boolean challenge with the ideal record key. Public metadata is fixed and equal between challenge branches. For every deterministic Boolean distinguisher, a measure-preserving tape involution proves exact equality of the hidden-root games and therefore advantage zero.

The checked modality results are:

- Active classical has zero advantage for both statically selected network actions: forwarding reaches the hidden honest root, while replacement under the fixed intended verification key fails ideal authentication and returns a challenge-independent failure observation. This replacement action does not cover a self-consistent attacker-owned identity and signed bundle, which production may accept because the advertised identity key is unsigned.
- Passive classical is the forward-only restriction and has zero advantage.
- Passive quantum capability has zero advantage after modeled recovery of the joint Ed25519/X25519 secret because the passive observer cannot replace the honest ML-KEM key and cannot decapsulate its ciphertext.
- Active quantum has advantage one for the identity distinguisher on the replacement action because forged classical authentication permits an attacker-selected ML-KEM key. The proof indexes the root oracle separately at the honest and substituted inputs and derives the removed record pad by recomputing from the exact accepted substituted input.

These are exact results for the closed finite ideal game, not reductions for the production primitive instances. The action is chosen outside the challenge game, and that closed game does not give the attacker a public-transcript API, direct KDF-random-oracle queries, record-tampering/decryption access, or a numerical hidden-input guessing bound. It covers neither multiple sessions nor arbitrary ratchet schedules, adaptive compromise, CTX composition, randomized interactive distinguishers, QPT computation, or QROM access. The executable SSProve package records the same bounded sampling computation, while the assumption-safe capstones use its direct finite pushforward to avoid a stronger infinite-sum interchange dependency in the pinned high-level library.

[`PqxdhRatchetRom.v`](../../beaconcrypt-core/proofs/ssprove/PqxdhRatchetRom.v) adds the missing bounded classical root/ratchet oracle interface for the positive cases. It fixes the four DH coordinates so the observer may guess them, samples only the honest ML-KEM atom and complete tagged table secretly, publishes the challenge ciphertext, and runs an arbitrary fuel-bounded adaptive query tree against that table. A table-dependent involution flips the pad without changing the hidden query that indexes it; any coupled decision mismatch therefore implies that the trace asked that exact query. The main capstone proves `Adv <= Pr[hidden-pad query]`, active-classical replacement is exactly zero, and `protocol_supported_scenario_root_hidden` checks that only active-classical forward/replace, passive-classical forward, and passive-quantum forward inhabit the wrapper. Active-quantum replacement remains exclusively the separate advantage-one theorem.

This extension does not turn the abstract result into a negligible bound. The one-bit symmetric domain can be exhausted with two queries, so the bad-event probability can be one; production-width parameterization and a guessing bound remain open. The program receives only the fixed public shape and challenge ciphertext, not the actual registration transcript or a record-tampering/decryption interface, and its oracle queries remain classical.

## Additional protocol-core computational hops

[`PqxdhEd25519EufCma.lean`](../../beaconcrypt-core/proofs/lean/BeaconcryptCore/Computational/PqxdhEd25519EufCma.lean) closes the one-target, one-successful-Phase-1-bundle field-origin authentication hop against generic VCVio weak EUF-CMA. It fixes the raw 32-byte Ed25519 public key, 64-byte signature, exact role-tagged X25519 and 1185-byte tagged ML-KEM messages, and attached `signature64 || message` shape. The honest wrapper signs prekey, one-time, then PQ; the source view verifies PQ, prekey, then one-time and parses identity, prekey, one-time, PQ. The bad event filters general source acceptance to the honest target public key and a changed decoded tuple. A deterministic first-changed-field selector produces one fresh valid message/signature pair without a guessed-field loss, so `Phase1FieldSubstitutionAdvantage` is at most one generic `SignatureAlg.unforgeableAdv.advantage` with coefficient one, no factor three, and no additive collision term. The wrapper has at most three logical signing-interface queries and every successful result logs exactly `[prekey, one-time, PQ]`; material and adversary base caps add as `qMaterial + qAdversary`, excluding opaque primitive-internal ambient work. Honest attached opening is kept separate under generic perfect completeness, deterministic verification, explicit runtime pure/bind evaluation laws, and an attached-API adapter premise. Protocol use is scoped by HB-62's one-successful-`Some` lifecycle; the Lean hop assumes one successful transcript and does not cover multiple successful bundles, multi-user lifting, attacker-owned self-signed identities, implementation refinement, strong EUF, QPT security, or the later related-key Ed25519-to-X25519 conversion.

[`PqxdhInitializerSecrecy.lean`](../../beaconcrypt-core/proofs/lean/BeaconcryptCore/Computational/PqxdhInitializerSecrecy.lean) composes the one-key ML-KEM endpoint with the hidden-root endpoint for one authenticated honest target and one initializer session. The real branch uses one honest encapsulation secret as the fifth coordinate of the exact `FF^32 || DH1 || DH2 || DH3 || DH4 || ss` input, while the terminal branch preserves the public KEM transcript, arbitrary correlated DH/context data, blocked challenge-decapsulation interface, and joint KDF projections but gives the caller an independent root. Its exact real-to-uniform-KEM-to-independent-root hybrid proves `Adv_initializer ≤ Adv_ML-KEM-IND-CCA + Adv_fixed-HKDF-joint + qRoot / 2^256` with coefficient one, a single replacement-secret draw, and no duplicated guess term. Source pre/post uniform, base, and logical decapsulation budgets stay separate; the KDF reduction has uniform upper bound `qKemAmbient + qUPre + qUPost + 32`, root upper bound `qRoot + 1`, symmetric upper bound `qSym`, total stream upper bound `qRoot + qSym + 1`, and zero untyped stream calls. The `qKemPrefix` and `qKemPost` components bounded by `qKemAmbient` are caller-supplied interpreter-budget slack justified by direct transformed-run bounds, not isolated source-query classes or concrete counts for opaque ML-KEM work. This is a KEM-to-independent-KDF initializer bridge, not a full symbolic-constructor interpretation: HB-54 collision conditioning, the explicit honest-target premise intended for future discharge by composing HB-63 with the symbolic result, multi-user lifting, related-key conversion, source refinement, and primitive security assumptions remain separate.

[`PqxdhInitialRatchetComplementarity.lean`](../../beaconcrypt-core/proofs/lean/BeaconcryptCore/Computational/PqxdhInitialRatchetComplementarity.lean) proves HB-67's deterministic initial-ratchet complementarity boundary with no probability loss. Its explicit `InitialResponseRefines` premise says that each endpoint's external interpreter returned the exact 64-byte HKDF response for that endpoint's own pending root/`INFO_R` request; the response type does not prove execution or correctness. The Server splits that response into send=`[0,32)` and receive=`[32,64)`, while the Beacon reverses the two roles. `ConcreteRatchetKernel.new` is checked to install those chains with zero counters, empty receive control/cache, and 50 empty material slots, and the reusable refinement theorem is parametric in an arbitrary wrapper-level `Ratchet.Crypto`. With separate root arrays and separate response objects, the capstone derives complementary zero/empty ideal states and both concrete cross-role chain equalities from a shared abstract root plus the two local response premises. Exact start/accessor facts expose the root input and `INFO_R` label. Under `ProductionHkdfPrefixConsistent`, a corollary rewrites the directional chains to `first32` and `second32` at one canonical `ratchetAddress root`; it neither introduces an extra `first64` joint-view query constructor nor identifies the wrapper crypto with `Pqxdh.ratchetCrypto`. Equal-root provenance, response execution, operation-level crypto refinement, later ratchet steps, protocol scheduling, and every primitive or probabilistic claim remain separate.

[`PqxdhInitializedChainsSecrecy.lean`](../../beaconcrypt-core/proofs/lean/BeaconcryptCore/Computational/PqxdhInitializedChainsSecrecy.lean) closes HB-69's one-session HB-66-to-directional-model-chain seam without introducing another assumption or loss. The downstream observer receives the public KEM transcript/context, the four conservatively known root coordinates, and only the model-byte chains `Server→Beacon=first32` and `Beacon→Server=second32`; it cannot inspect the root, KEM secret, extracted arrays, or kernels. The wrapper makes the two existing typed projections in that order at the same canonical `ratchetAddress`, and a checked random-world normal form shows one empty-cache 76-byte sample, one same-address hit, and a populated cache retained for adaptive observer queries. Thus there are two logical symmetric view invocations and conservatively two challenger-facing stream invocations, not two independently sampled values. The production game rewrites to `Pqxdh.rootChains` under the existing prefix-consistency contract, the terminal game uses the two projections of one independent-root lazy stream, and the capstone retains exactly one ML-KEM IND-CCA advantage, one fixed-HKDF joint-stream advantage, and `qRoot / 2^256`. Its explicit upper bounds replace observer `qSym` by `qSym+2`, replace stream `qRoot+qSym+1` by `qRoot+(qSym+2)+1`, and preserve all other HB-66 classes. HB-67 is applied only as a deterministic conditional corollary under separate endpoint-local root/response premises. Projection-collision conditioning, target/root/response provenance, operation-level source refinement, later ratchet steps, multi-session lifting, and quantum protocol interpretation remain separate. The fixed primitives remain named external assumptions, not future implementation or security proof work.

[`PqxdhHybridSecurity.v`](../../beaconcrypt-core/proofs/ssprove/PqxdhHybridSecurity.v) makes a hybrid-combiner hop explicit. It samples one selected coordinate of the ordered `DH1 || DH2 || DH3 || DH4 || ML-KEM` input secretly and treats every other coordinate as a public parameter. A bounded adaptive distinguisher for a one-bit payload masked by the ideal root output has advantage at most the probability of asking the modeled five-coordinate input containing the hidden contribution. The scenario corollaries select the ML-KEM contribution for active-classical, passive-classical, and passive-quantum forwarding. The passive-quantum capability exposes all four DH coordinates, while active-quantum replacement has no hidden coordinate and therefore receives no positive corollary. The model now uses `ProtocolLabels.v`'s proved-injective encoding of the exact production strings but still collapses fixed padding and HKDF-SHA-512 Extract/Expand into an ideal lookup; it has no extra session tag and does not prove real-or-random production root-key pseudorandomness.

[`RatchetForwardSecrecy.v`](../../beaconcrypt-core/proofs/ssprove/RatchetForwardSecrecy.v) adds a one-step post-erasure game. The public view contains the compromised next chain, predecessor challenge ciphertext, and predecessor nonce; the predecessor chain and message key are absent. The table involution flips only the predecessor message-key output while preserving the next-chain and nonce outputs from the same `key || next_chain || nonce` expansion. The checked capstone gives `Adv <= Pr[query erased predecessor-chain input]`, and a separate theorem bounds the public query trace by the supplied fuel. Its finite domain tag now comes from the exact-label module and applies only to the genuinely different root and symmetric production labels, not initial-versus-step symmetric expansion. This is conditional on erasure, has no session/direction/sequence or multi-step composition, and does not establish implementation zeroization or post-compromise healing.

[`RecordIntegrity.v`](../../beaconcrypt-core/proofs/ssprove/RecordIntegrity.v) adds a finite one-record/one-candidate game that assumes one ideal random-function authenticator for the combined direction-specific AEAD and modified CTX acceptance decision. This avoids attributing sequence or CTX sender-identifier binding to the AEAD tag alone, but it does not prove that AEAD and CTX compose. A broad active modification is exactly partitioned by whether its authenticator input was previously queried, while the fresh-input and fresh-guess predicates are definitionally identical; these are classification facts rather than a primitive reduction. Reusing the same protected payload under a distinct context or sequence extracts a same-run unequal-input/equal-output table witness. The game excludes identical-context replay and supplies neither a standalone collision experiment nor a general verification-oracle composition theorem.

[`RecordIntegrityBound.v`](../../beaconcrypt-core/proofs/ssprove/RecordIntegrityBound.v) proves the quantitative finite fresh branch. A table-dependent involution flips only the completed candidate's unqueried entry while preserving the honest payload and bounded adaptive adversary run. It pairs success with rejection, and `record_uniform_one_bit_fresh_tag_guess_bound` proves `Pr[fresh accepted candidate] <= 1/2` as a direct uniform-table probability evaluating the exact existing verifier runner. No equality to the named distribution-valued record game is checked. The theorem is specific to that ideal combined authenticator and gives neither the production tag-width bound nor a composition of independent AEAD and CTX assumptions.

These are standalone finite reductions over one-bit atoms. They improve the computational decomposition of the cryptographic core, but they do not yet compose hybrid establishment, arbitrary ratchet histories, record integrity, and CTX into one arbitrary multi-user theorem. Production-width bad-event estimates, adaptive corruption, randomized interactive adversaries, QPT lifting, and QROM access remain future work.

[`StateSeparatedComposition.v`](../../beaconcrypt-core/proofs/ssprove/StateSeparatedComposition.v) now supplies a structural milestone between those standalone reductions and the planned common experiment. It links keying and keyed sibling packages through a private consuming CKEY provider, checks role/session/provenance on one destructive transfer, uses the production `(L,R)/(R,L)` orientation and left half at sequence one, and proves a pure authenticated single-run equality with a monolithic key calculation. Its redesigned public operation takes `unit`, samples the complete eight-bit finite tape internally in one joint draw, discards the opened plaintext, and returns only `Some ciphertext` or the explicit false-provenance result `None`; a challenge-indexed game pair and direct finite pushforward use that view without claiming package-distribution equality. It also records explicit construction-failure, drop, success, and pending-beacon rejection snapshots. The one-bit response omits nonce, associated data, AEAD, CTX, identifier checks, and serialization; the lifecycle snapshots are not connected reachability theorems; the API has no active scheduling or reveal operations; and no package-interpreter contextual equivalence or advantage bound is claimed.

[`StateSeparatedPackageSemantics.v`](../../beaconcrypt-core/proofs/ssprove/StateSeparatedPackageSemantics.v) checks the next finite seam without claiming stock contextual equivalence. A restricted interpreter supplies one indexed value to the exact linked `RUN` body's sole joint sampler and executes its raw code from SSProve's canonical empty heap. Local bridges establish that staged proof-erased raw projections are the computational fields of the checked linked packages, and indexed certificates carry equality from those raw executions to kernel-reduced normal forms. Authenticated execution returns the direct `Some ciphertext` observation and leaves both private CKEY slots as cleared `Taken` tombstones; false provenance returns `None` before sampling or invoking the core and leaves both slots `Empty`. After the heap is hidden, the public observation agrees pointwise with the direct state-separated calculation, and its uniform finite-source decision probability and distinguishing advantage are equal for every deterministic predicate. The executed package bodies retain the exact distinct PQXDH-root domain, the intentionally shared initialization/step symmetric domain, and the 32/64/76-byte uses; the certificate introduces no extra role, direction, sequence, or initialization/step tag.

[`StateSeparatedIndexedSessions.v`](../../beaconcrypt-core/proofs/ssprove/StateSeparatedIndexedSessions.v) closes that immediate second-call mismatch against the right stateful reference rather than against the stateless monolithic harness. It gives each of two Boolean handles distinct server and beacon CKEY slots, while the outer driver caches the one complete finite ROM sample used by both handles. The first fresh authenticated call consumes and stores a sample, the fresh opposite handle reuses it without consuming the residual stream, and false authentication or same-handle reuse returns `None` with exact heap and stream neutrality. Its adaptive equality allows the first optional ciphertext to determine the second handle and matches the stateful reference on both observations and remaining samples. A separate sample per handle would be unsound within the two Boolean root/symmetric buckets because equal KDF queries could receive different answers; the single cache preserves table consistency. Handles select only ghost locations and per-handle static inputs and are not added to the exact root label, shared initialization/step symmetric label, direction, sequence, or KDF input.

The assumption boundary remains deliberate. With pinned SSProve 0.2.4, an assumption-reported capstone that retains generic package or raw sampler syntax traverses a generic distribution proof containing the repository-rejected infinite-sum interchange dependency. The modules therefore check exact raw executions locally and use proof-erased or direct finite statements rather than stock `Pr`, `Pr_op`, package-validity, interchange, or unrestricted contextual-advantage semantics. The two-handle relation does not model production registration replay, replay identifiers, forks, rollback, active registration/delivery, arbitrary multi-user sessions, production-width primitives, intended-peer authentication, the four-corner reduction, or the implementation bridge.

The [SSProve end-to-end composition investigation](ssprove-end-to-end-composition.md) turns the remaining gap into a concrete architecture and records three checked milestones: PQXDH becomes a keying package, the ratchet and record transform become a keyed package, a private session-indexed key interface connects their disjoint states, the exact finite one-call execution is certified, and two bounded handles are related to a stateful adaptive reference. It specifies the remaining four-corner state-separating hybrid, a single prefix-consistent HKDF package with the exact labels above, the required collision events, active public and private protocol interfaces, deterministic representation bridges, and phased acceptance criteria. It is a design plus checked bounded seams, not a claimed end-to-end computational composition theorem.

## Modified CTX commitment games

For key `K`, nonce `N`, 153-byte associated data `A`, transmitted 16-byte AEAD tag `T`, sequence `S`, and sender identifier `I`, production builds the following 229-byte transcript and protected payload:

```text
X = K || N || A || T || LE64(S) || LE64(I)
U = BLAKE2b-512(X)
R = C || T || U
```

A CTX misattribution event is one fixed protected payload `R` with two accepted explanations that differ in at least one of `K`, `N`, `A`, `S`, `I`, or accepted plaintext. The base AEAD is allowed to open the same `C || T` under distinct keys or contexts, so the binding claim does not smuggle in key commitment from ChaCha20-Poly1305.

[`BoundedRom.v`](../../beaconcrypt-core/proofs/ssprove/BoundedRom.v) supplies a generic fuel-bounded adaptive classical random-oracle tree whose complete finite function table remains hidden from the program and observation. [`CtxGame.v`](../../beaconcrypt-core/proofs/ssprove/CtxGame.v) instantiates that runner with an adversary that can make at most `q` hash queries and return one protected payload with two explanations. The verifier performs exactly two additional hidden-table queries, checks both outer digests and both calls to an arbitrary deterministic multi-opening AEAD, and extracts the two transcript-and-digest pairs.

The checked game proves:

```text
Pr_bounded-hidden-ROM[CTX misattribution] <= Pr_same-run[unequal-input, equal-output collision].
```

`ctx_hidden_rom_extractor_reduction` proves the generic inequality for any hidden-table distribution, and `ctx_uniform_hidden_rom_extractor_reduction` specializes it to a uniformly sampled finite random function. `ctx_hidden_binding_trace_size_bound` accounts for at most `q + 2` completed oracle queries, while `ctx_attach_verifier_completed_run` proves that a bounded completed adversary trace is preserved and followed by exactly the two verifier queries. A concrete deliberately multi-opening AEAD with a colliding constant table makes the misattribution event executable and non-vacuous. The earlier [`CtxEventReduction.v`](../../beaconcrypt-core/proofs/ssprove/CtxEventReduction.v) remains as the more generic same-execution event-inclusion lemma.

[`CtxPrivacy.v`](../../beaconcrypt-core/proofs/ssprove/CtxPrivacy.v) keeps confidentiality separate from binding. It represents the real digest by jointly sampling a uniform hidden key, finite random-oracle table, and fresh value and programming the table at the key-derived transcript. A key-preserving swap establishes the true-real/programmed-real representation, the same-run fundamental lemma makes every programmed/fresh decision mismatch imply a classical query for the secret transcript, and `ctx_hidden_uniform_key_true_real_privacy_bound` bounds the absolute true-real/fresh-ideal decision gap by that bad-query probability using direct finite-source reindexing.

The CTX binding proof needs no additive AEAD-security term, but it supplies no numerical collision bound for BLAKE2b-512. The privacy hop is conditional on a hidden uniform record key and an independent ideal-AEAD confidentiality theorem; it supplies neither the probability of guessing a production-width key nor a complete transformed-AEAD composition theorem. Both files use one-bit finite atoms to make exhaustive random functions available to SSProve, so a representation and width-parameter bridge remains required before claiming the production game.

The locked suite uses Rocq 9.0.0, SSProve 0.2.4, and MathComp 2.4.0. The build compiles and kernel-checks every repository-owned module, exact-diffs every assumption report, and rejects repository admissions, the unsafe hax prelude, and the pinned library's admitted infinite-sum interchange theorem. The accepted foundational assumptions are propositional extensionality, dependent functional extensionality, constructive indefinite description, and an abstract `realType`.

### Cross-prover deterministic contract

The game-level CTX transcript is an injective product of its semantic bit fields, which lets SSProve prove the extractor soundness without assuming implementation correctness. Its intended connection to production is the combination of these checked F* results in [`Beaconcrypt_core.Commitment.Lemmas.fst`](../../beaconcrypt-core/proofs/fstar/Beaconcrypt_core.Commitment.Lemmas.fst):

- `production_commitment_input_is_injective` establishes injectivity of the exact six-field 229-byte transcript, including both little-endian integers.
- `ctx_distinct_openings_imply_hash_collision` constructs unequal transcripts with equal hash outputs from any two semantically distinct accepted explanations of one fixed payload, for arbitrary pure hash and AEAD-open functions.

Cross-prover composition still requires a reviewed representation bridge showing that the SSProve semantic fields represent the parsed production explanations expected by the F* theorem, that both checks correspond to the modeled acceptance predicates, and that the abstract transcript constructor corresponds to the proved fixed-width byte encoding. The bridge is an integration obligation, not a request for SSProve to verify Rust correctness.

Collision resistance establishes only CTX binding or misattribution resistance. It does not by itself prove that publishing an unkeyed hash whose input includes secret `K` preserves record privacy or ordinary ciphertext integrity. Those composition claims need a separately stated transformed-AEAD, random-oracle, secret-input, or equivalent assumption that is suitable for the classical or quantum attacker class being claimed. Ordinary modifications to `C` remain covered by the base AEAD integrity argument rather than by the same-payload CTX collision reduction.

## Hax coverage and SSProve extraction gate

The ProVerif model uses the hax extraction boundary for six production PQXDH data types and three production-used builders: `registration_id`, `build_root_key_input`, and `build_associated_data`. The three functions carry source-local ProVerif replacement annotations because ProVerif needs ideal constructor semantics, and the build rejects extraction placeholders or disappearance of those named items. The canonical production transcript interface is a handwritten CI-synchronized declaration/manifest consumed by the scenarios, not generated extraction. Cryptographic equations, role processes, events, state tables, record schedule, attacker controls, and queries remain handwritten because they are security specifications rather than executable Rust behavior.

Direct hax-to-SSProve extraction was probed for `build_commitment_transcript` and related deterministic helpers, but it is not currently an admissible proof dependency. The generated module imports hax's bundled Hacspec prelude and aggregate library, and the pinned prelude exports `Axiom falso : False` for unchecked result unwrapping. Importing that axiom would make every proposition provable and invalidate the trusted computational proof. The selected generated commitment code also requires backend support for Rust array construction that is not yet emitted as a complete standalone SSProve definition.

The SSProve suite therefore imports neither hax-generated Rocq nor the Hacspec aggregate library. This is an explicit safety gate, not a decision to abandon extraction. Direct extracted code can replace a deterministic contract only after all of the following conditions hold:

1. The exact hax, Rocq, MathComp, and SSProve versions are pinned and version-checked.
2. The deny-all hax selector emits only the reviewed production-used items and their necessary dependencies.
3. The generated modules compile without hand edits or unresolved identifiers.
4. No imported path exposes `False`, unchecked unwrap axioms, admissions, disabled guard checking, or an equivalent proof bypass.
5. Regeneration is deterministic and CI rejects generated drift.
6. The generated representation is shown to match the game interface used by the computational theorem.

Until this gate passes, the sound route is to state the handwritten finite model explicitly, mirror only named production operation shapes, and keep the F*/Lean-to-SSProve representation bridge as a named open obligation. Handwriting an executable look-alike of production and silently calling it extracted is not acceptable.

## Assumption ledger

The eventual protocol theorem must state assumptions per attacker class rather than use one undifferentiated “secure primitives” premise.

| Component | Active classical requirement | Passive quantum requirement | Important limitation |
| --- | --- | --- | --- |
| Ed25519 authentication | HB-63 gives a coefficient-one generic weak-EUF-CMA reduction for one target identity's three exact Phase-1 fields and one successfully returned bundle. The final protocol theorem still needs source-level protocol wiring and multi-user application; detached/attached verification and the related Ed25519-to-X25519 use are governed by permanent primitive/API assumptions. | It may be treated as completely broken for transcript confidentiality. | HB-63 proves no Ed25519 internals or QPT claim, does not exclude attacker-owned self-signed identities, and does not simulate the related-key conversion. |
| X25519 contributions | The exact active key-agreement assumption needed for the four DH contributions, including public-key validation and all-zero rejection behavior. | Every classical DH contribution may be treated as known. | Classical DH secrecy supplies no harvest-now-decrypt-later protection. |
| ML-KEM-768 | HB-56 gives the exact one-key source-shaped real-to-uniform branch distance as one generic VCVio KEM IND-CCA advantage, and HB-66 composes that branch into the one-session initializer while preserving the selected key under its explicit authentication premise and the exact challenge ciphertext. The eventual protocol result still needs selected multi-user or protocol-specific wiring and must explicitly apply the permanent public-key-binding assumption when its symbolic scope uses that property. | QPT security is a permanent primitive assumption for the passive-quantum claim, while an explicitly supported multi-epoch extension would also apply the binding property or authenticated key binding. | Generic IND-CCA does not imply public-key binding; HB-56/HB-66 prove no ML-KEM implementation internals or QPT lifting, and passive survival does not stop active substitution of an attacker-owned ML-KEM key. |
| Hybrid root HKDF | HB-66 gives a coefficient-one one-session composition from the honest KEM secret through the exact ordered four-DH-plus-KEM input to an independent root, assuming the named fixed-HKDF joint-stream advantage and paying `qRoot / 2^256`. | A quantum-suitable interpretation of the same named primitive assumptions and a protocol-level QPT lift remain required when the four DH inputs are known. | HB-66 leaves arbitrary DH/public correlation intact but does not prove HKDF internals, HB-54's source-observed projection-collision conditioning, multi-session composition, or the source-to-symbolic constructor bridge. |
| Symmetric ratchet KDF | HB-69 composes HB-66 through the two same-address initial `INFO_R` projections to directional model chains, and HB-67 conditionally supplies the opposite-role zero-state kernel witnesses under explicit equal-root and response premises; the eventual protocol theorem still needs the HB-54 constructor-collision composition and protocol provenance/schedule wiring, while key-evolution and one-way properties are permanent primitive assumptions. | Quantum-suitable primitive properties are permanent assumptions for any passive-quantum claim. | HB-67/HB-69 prove no HKDF implementation/security, and no post-compromise security follows from a one-way chain without fresh secret input. |
| ChaCha20-Poly1305 | The exact nonce-based privacy and integrity notion under the protocol's key and nonce discipline. | A quantum-suitable classical-interface notion for passive transcript confidentiality. | Primitive correctness and implementation side channels remain out of scope. |
| Modified CTX/BLAKE2b-512 | Collision resistance for binding, plus an explicit CTX privacy and ordinary-integrity preservation assumption for record composition. | Quantum collision resistance for binding and a QROM or other quantum-suitable preservation assumption for privacy. | Collision resistance alone does not show that publishing `H(K || ...)` preserves secrecy or ordinary integrity. |
| State and randomness | Fresh unbiased key generation, one authoritative state owner, atomic replay consumption, no rollback or fork, and correct peer routing. | Honest unmodified delivery and retained ML-KEM secret state. | Replication, rollback, compromise timing, erasure, and availability need separately scoped models. |

Primitive correctness, primitive implementation realization, compiler correctness, side-channel resistance, machine-code correspondence, and every invoked primitive security, binding, generator-independence, quantum, and related-use property are permanent explicit assumptions outside this proof program; they are not future proof obligations. In-scope work proves only BeaconCrypt protocol reductions, source wiring, state facts, and query accounting under those assumptions.

## Remaining computational protocol work

The following work remains before beaconcrypt has a complete computational protocol proof:

1. Extend the one-session, one-record game to adversary-facing registration, response, record sending, record receiving, compromise, replay, and bounded-concurrency interfaces with the actual public transcript and failure leakage.
2. Generalize the finite CTX fields to production-width types, use the maintained source/Lean boundary required by the specific CTX seam, and connect the resulting protocol collision event to the permanently assumed named primitive collision game with exact hash-query and reduction-overhead accounting.
3. Compose HB-69's one-session directional-chain game with HB-54's source-observed same-width collision condition without duplicating its fixed-HKDF advantage, and then connect the resulting constructor view to the symbolic initializer. Supply authenticated-target, equal-root, and response-execution provenance before invoking HB-67's conditional concrete-kernel endpoint; do not synthesize concrete arrays from model bytes or add a `first64` joint-view constructor. Extend the result to the later symmetric-ratchet uses required by the protocol schedule.
4. Lift the closed active-classical game to multi-user handshake secrecy and one-way agreement under explicit ideal or reduced Ed25519, X25519, ML-KEM, and HKDF interfaces. Reuse HB-63's one-target field-origin EUF-CMA endpoint and HB-66's one-session KEM-to-independent-root composition without claiming either supplies the multi-user lift; prove the protocol wiring that applies the permanent attached-verification, Ed25519-to-X25519 related-use, and ML-KEM public-key-binding assumptions rather than deriving those primitive properties. Preserve the dropped-response counterexample to converse agreement.
5. Prove ratchet record privacy, authenticity, replay resistance, direction and peer separation, and intended scoped forward secrecy for arbitrary bounded schedules rather than one fixed sequence-zero record.
6. Compose the CTX programming hop with ideal AEAD confidentiality, and prove ordinary-integrity preservation separately from CTX binding so collision resistance is never used for the wrong property.
7. Add an ML-KEM-removal or reveal negative control to the honest-only passive suite, and state and apply the permanent QPT/QROM primitive assumptions explicitly at the protocol boundary before promoting the passive-quantum capability result.
8. Retain both active-quantum attack witnesses as required negative controls and state active post-quantum security as unsupported until the wire protocol authenticates all key-establishment material with a quantum-secure mechanism.
9. Maintain the SSProve proof-policy, exact assumption reports, out-of-tree objects, `coqchk`, CI, and trust-boundary inventory gates for every future full-game or extraction artifact.

Completion means checked games and reductions with named assumptions and losses, not merely four passing labels in a fixed ideal game. The supported summary is now: beaconcrypt has a four-way symbolic suite, bounded hidden-ROM CTX binding and privacy hops, a one-session one-record closed ideal game with three exact zero-advantage results and the expected active-quantum advantage-one break, an adaptive classical-ROM reduction of the positive cases to an explicit bad-query event, a production-width one-session initializer-to-directional-chain reduction to one ML-KEM IND-CCA advantage, one fixed-HKDF joint-stream advantage, and `qRoot / 2^256`, a conditional deterministic bridge from equal initial roots and faithful 64-byte responses to complementary zero-state role kernels, and a source-synchronized finite witness that a genuine sequence-3 registration frame can complete through the general receive path with the exact bounded ratchet poststate while an otherwise identical first-only counterfactual cannot commit. Arbitrary-execution and multi-user composition, response/root/target provenance, HB-54 collision composition, the complete source-to-symbolic constructor bridge, and quantum-computational lifting remain work in progress.

## Maintenance and verification

The ProVerif result checker pins the exact number and classification of queries for each named scenario, so an unexpected proof, attack, timeout, inconclusive result, or missing query fails the build. Some correspondence checks currently include ProVerif-generated variable suffixes and are therefore safe against false acceptance but brittle across harmless pretty-printer changes; structural or normalized matching should replace those exact suffixes. The CTX/no-CTX weak-AEAD differential remains a separate required negative control: the double-opening query must be unreachable with CTX and reachable when only the commitment checks are removed.

Run the focused checks in the locked proof shell before changing the status in this report:

```bash
make -C beaconcrypt-core check-proverif-transcript-fidelity
make -C beaconcrypt-core check-proverif
make -C beaconcrypt-core check-ssprove
```

Run `make -C beaconcrypt-core verify` for the repository-wide proof gate once the independent Lean/F* extraction reorganization is healthy. A failure confined to those correctness backends must be reported to their owners, but it must not be hidden by weakening the computational or symbolic checks.

The HB-65 validation checkpoint used `nix --extra-experimental-features 'nix-command flakes' develop --no-update-lock-file 'path:.#proofs'` and the existing Makefile shell override `SHELL=/run/current-system/sw/bin/bash`. The locked toolchain gate passed; `check-proverif` passed all 28 scenarios and the complete 13-test fidelity suite, including all 2,861 mutations; `cargo test --locked -p beaconcrypt-core --lib` passed all 77 core tests; `check-ssprove` passed its exact assumption reports and `coqchk`; and `check-lean` completed the maintained root and all-module build with 3,097 jobs. Regenerating the ProVerif and Lean extraction produced byte-identical tracked artifacts and no untracked generated source. The F*/Lean admission policies, reviewed inventory, whitespace checks, and `cargo fmt --all -- --check` also passed. Workspace-wide Clippy was attempted but could not run because the locked Rust 1.93 nightly does not meet the public adapter's declared Rust 1.96 requirement. This focused checkpoint does not claim that the unrelated historical F* re-extraction/backend problem or the repository-wide `verify` gate is repaired; no primitive, protocol behavior, source compiler requirement, or proof policy was changed to obtain these results.
