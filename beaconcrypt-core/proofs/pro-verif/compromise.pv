(* SPDX-License-Identifier: 0BSD *)

process
  out(c, ed_public(SERVER_IDENTITY_SECRET));
  (
    !HonestBeacon(BEACON_RECORD_SECRET)
    | !Server(INITIAL_SECRET, CACHED_SECRET, ADVANCE_SECRET, FUTURE_SECRET)
    | !CompromiseBeaconState()
  )
